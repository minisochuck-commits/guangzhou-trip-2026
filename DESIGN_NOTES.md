# 设计记录 · 广州行程分享页

## 本次交付验收（2026-09-10）

Owner 对本次横向字段表完成实际浏览器检查：390px 中文与阿语、360px 英文；全部视图45行、Li/Qiuting18行、其余单人各9行，列头始终是日期/人员、住宿、活动、餐饮、交通。日期跳转、末日可见、重复点击跳回、筛人后横向归零均已检查。阿语日期导航保持选中日可见，详情弹窗为RTL、全屏宽且有44px关闭按钮。

横滑时固定首列与表头实测保持位置，首列背景为不透明 rgb(255,241,243)，无文字透叠；整页宽度未超出360/390px，横向滚动限表格内部。反向验证：初版半透明固定列在实际横滑中出现文字透叠，已据此拒绝并更正；旧四行卡片结构被移除，当前浏览器单一表格列头检查可识别方向回退。

已实际打开酒店中文地址、Reham 9/22自由行建议（仅黄埔古港/花城广场）、9/27航班与行李（MS959、23:20至次日04:50、T3、托运2×32kg/手提2×8kg）。事实数据两文件与起点提交dea812ea8e162f4ae2f1c06d824a35ff1b0337b4逐字一致。未将旧版展示验收当成本次证据；复制地址保留原实现与可手选中文，未声称已读回系统剪贴板。

产品定位：**一张精致好用的行程单**，不是营销落地页。手机优先，看的人是 5 位出差的同事。
没有大 hero、没有视差、没有 CTA。

> 展示结构已被替代过几轮。**当前生效的是「横向字段表」**（下一节）。
> 长事件流、独立「吃住行」tab、按组合并的补充卡、全局待确认清单、
> 以及**每人一张四行小表**都是历史方案，不要再当规则参考。
>
> 更正一处旧记录：上一版曾把「每人一张四行小表」写成用户最终拍板 —— 这是错的。
> 用户后来明确说自己当时的行列措辞有误，并拍板改成下面这张表。

## 主结构：一张横向字段表（当前生效）

**字段做列头，每一行是「某天的某个人」。** 所有日期、所有人共用同一张表。

| 固定首列 | 住宿 | 活动 | 餐饮 | 交通 |
| --- | --- | --- | --- | --- |
| 日期 + 人员（纵向两行） | … | … | … | … |

- **选人只筛行，不转置。** 表的形状永远一样，换人只是少几行。
- **日期按钮是跳转**，不是「只显示这一天」：点一下把表格滚到那一天的第一行，
  同时更新选中日期（那几行带浅红底）。筛过人之后日期列表只剩他有安排的日子，
  跳过去也一定落在有内容的行上，不会停在表格底部的空白。
- **固定表头 + 固定首列**：横向滑动看四个字段，纵向滑动走日期，日期和人始终看得见。
- 一共 45 行（18 天 × 各自的人员），全部来自 `cardsForPerson(person, date)`。

### 为什么滚动面板要套在 shadcn 的容器上

shadcn `<Table>` 自带一层 `[data-slot="table-container"]`，只有 `overflow-x-auto`。
按 CSS 规则，`overflow-x: auto` 会让 `overflow-y` 也变成 `auto`，所以**它才是最近的
scrollport**；如果在外面再套一个纵向滚动的 div，sticky 表头会跟着内容一起走。

解决办法是不改 vendored 组件，而是从外层用 `.matrix-scroll [data-slot="table-container"]`
给那一层加 `max-height` 和 `overflow: auto`（见 `app/globals.css`）。
一个容器同时管上下左右，固定表头和固定首列才可靠。

日期跳转也只操作这个容器的 `scrollTop`（减去表头高度），不碰窗口纵向滚动 ——
否则页面的 sticky 顶栏会把目标行盖住。

### 谁合并、谁不合并

Ahmed 与 Mohamed 同房、同活动、同交通，数据里本来就是一条记录，全部视图合并成一行。
Li/Qiuting、Rahma、Reham 各自一行。

选中单人时，首列只写选中的那个名字（不再写「Ahmed · Mohamed」）；
**室友关系仍然由住宿正文体现**，不靠首列表达。

**Rahma 和 Reham 不是「接待组」**。早期版本把她们当成全程同吃同住的一组，是错的：
Rahma 9/25 换酒店、9/27 白天走；Reham 一直住洲际、9/27 深夜才走。合并会直接写错事实。

### 详情为什么进 Sheet

列宽只有 13.5rem。航班块、完整中文地址、三条路线在格子里就地展开会把整行撑到满屏，
横向滑动时其他列全是空白。所以格子里只留**摘要 + 待定提示 + 一个「详情」按钮**，
点开是底部 Sheet（复用已有组件），里面按顺序放：摘要 → 细节 → 可复制中文地址 →
航班与行李 → 自由行建议。Sheet 宽度不受列宽限制，三语都读得完整。

单元格内容逻辑就是原来 `PlanRow` 那一套，搬进了 `components/trip/plan-cell.tsx` 的 `PlanCell`。

Sheet 是通过 Portal 挂到 `<body>` 的，**拿不到表格那边的 `dir` / `lang`**，
所以 `SheetContent` 上显式写了一遍，否则阿语内容会按 LTR 排。
自带的关闭按钮只有 16px、纯英文、还压在标题右上角，已经 `showCloseButton={false}` 关掉，
换成一个 ≥44px、带中英阿文案的 `SheetClose`；标题区 `min-w-0 flex-1` 给它让位。
表头吸顶，航班和路线展开后关闭按钮不会滚出去。

### 自由行建议挂在活动格

`freeTime` / `freeRoutes` 的过滤没有变，只是入口从「当天页面底部一个折叠」
挪进了**那一行活动格的详情 Sheet**。这样建议永远绑在具体的人和具体的那一天上：
9/22 只有 Reham 那一行有，Li/Qiuting 在中国的个人行程那几行不会出现广州路线。

## 状态只有两档，而且只标「待定」

`confirmed`（已定）/ `pending`（待定）。早期的三档里「建议安排」和「待确认」在实际内容里
分不清，反而让人以为「建议」比「待确认」更接近落实。现在只问一句：**这件事定了没有。**

**已定的不挂徽章。** 四行里每行都挂一个深色「已确定」既抢走内容列的宽度，又是纯噪音 ——
已定就是默认状态，看正文就行。只有 `pending` 才在**摘要下面**单独起一行显示「待定」，
不跟正文左右分列（阿语的待定文案更长，分列会把内容挤成一条竖线）。

没定的一律 `pending`，并且在正文里写清**待定的是什么**（是酒店名字没定，还是出发时间没定）。

## 联系谁，写在摘要里

交通行的摘要必须同时给出**到机场时间**和**找谁对接**，不能把人名藏进折叠：
Rahma 向董事长确认、Ahmed / Mohamed 向 Rahma 确认、Reham 由公司送机且 Rahma 提前协调。
只有车辆和出发时间这类还没落实的细节才进「详情」。

其余四人 9/21 落地后的市内接送**没有指定负责人**，就照实写「待确认」，不臆测谁去接。

## 姓名

正文统一正常大小写：Li/Qiuting、Rahma、Ahmed、Mohamed、Reham。
全大写的票面姓名读起来像喊人，且在阿语版里更难辨认，所以**只留在「人员详情」折叠里**。
航班号和机场码保持大写，那是标识符不是名字。

代码里 Mohamed 的 id 仍是 `hassan`（票面首字段）。改 id 会牵动所有 `people` 数组，
收益只是好看，风险是漏改，所以不改。

## 事实纪律（产品要求，不要"优化"掉）

- 航班的日期、时刻、航站楼**一律按票面**，任何情况下不改。
- 机场提前到达统一 3 小时（用户拍板），**成都中转不再套 3 小时** —— 中转是衔接，不是重新到机场。
  MS958 跨零点，出发去机场是 9/20 当晚，这一条单独写死，不走模板拼接。
- 行李额写具体数字和来源，不再用「票面没标公斤就不猜」占位。核实日期写在页面上。
- 酒店名称未定就**不给地址、不给导航**。换酒店后明确提示重算出行时间。
- Reham 的巡店场次在 9/23 与 9/24 之间**择一**，两天都不能写成确定参加。
- Ahmed / Mohamed 的巡店学习**没有固定门店、没有固定课时**。此前写的「黄埔云汇天地」
  只是原申请里的参考，不是已确认的地点，页面上已经拿掉。
- Reham 不是全程公司包餐：会议餐由公司安排，**外出自由活动时可以不参加**。
- 9/21 迎接当天的用餐时间没给，就写没给。
- Li/Qiuting 的离穗日期与所在城市没给，页面**不推断她已经到家**。
- 不保留「谁接手 Reham 未确认」这类旧警报 —— 现在明确了由公司安排、对接 Rahma。
- 出发前准备一律写成「准备 / 测试」，不写「已安装、已充值、已实名」。
  ChatGPT 在中国大陆不在官方支持范围，装了 VPN 也不保证可用，所以同时给离线翻译和
  可复制中文短句作为备选 —— 一句说明，不训人。

## 自由行建议

三条半日路线（黄埔古港 / 西关老城 / 花城广场夜景），全部来自已核实的官方来源，
**未预订**，时长写成「规划参考」，不写未核实的票价和营业时间。

出现位置是刻意控制的：

- **日程页**：只有当天真正自由的人才出现「自由时间可以去哪」折叠入口，
  **标题里点名是谁**（例如 9/22 只写 Reham）—— 当天还在开会的人不该以为全团都自由。
- **只有半天自由就只给半天能走完的路线**：`PersonDayCard.freeRoutes` 限定 id，
  `RouteList` 接一个可选的 `ids` 过滤参数，不复制第二份路线数据。
  9/22 Reham 只有下午自由，所以只给黄埔古港和花城夜景 —— 西关老城要占一整个白天，
  会跟上午的会议撞。
- **Li/Qiuting 在中国的日子不显示广州游玩** —— 她不在广州。
- **指南页**给完整总览，任何人都能翻。

同一天有多个自由的人时只渲染一份路线折叠，不给每个人各挂一份。

## 图片

只保留 `public/reference/hq-notice.png`（总部总通知原件，折叠在页尾），
说明写明「本团实际安排以本页每天的表格为准」。研究里的官方景点照片**未取得再使用许可**，
所以不放；路线用文字表达就够。

## 排版与 RTL

- `html { font-size: 16px }`，正文 16px，标签不低于 14px，可点区域不低于 44px。
- 逻辑属性（`ps-*` / `ms-auto` / `before:start-0`），RTL 自动镜像；箭头 `rtl:rotate-180`。
- 航班号、机场码、时间、姓名包 `.ltr-token`（`direction: ltr; unicode-bidi: isolate`）。
- 日期导航只滚横向容器（`scrollBy` + 可视坐标差），不用 `scrollIntoView`，
  否则会连带滚动窗口纵向，把当天标题顶到 sticky header 后面。
- `html, body` 保留 `overscroll-behavior-x: none` 与 `overflow-x: clip`。

## 表格类名要显式覆盖 vendored 默认值

公共 `Table` 组件给 `<td>` / `<th>` 默认加了 `whitespace-nowrap`、`h-10`、`text-left`、
`align-middle`，直接用会让单元格不换行、高度被压。矩阵里显式覆盖：
`whitespace-normal break-words`、`h-auto`、`text-start`（RTL 才对）、`align-top`。

`<Table>` 本身加 `border-separate border-spacing-0` —— Tailwind preflight 把 `table`
设成了 `border-collapse: collapse`，而 collapse 下 **sticky 单元格的边框不会渲染**，
所以边框全部画在单元格上（`border-b` / `border-e`）。表宽用 `w-auto` 而不是 `w-full`，
列宽靠 `min-w-*`：首列 7rem（112px，约占 390px 屏的 28%，不撑成半屏），
四个字段列各 13.5rem。

sticky 单元格必须自带**不透明**背景色。真机 QA 踩过一次：选中日期那几行原来用
`bg-miniso-red-tint/50`（半透明），横滑之后活动/餐饮列直接从固定首列的姓名底下透出来。
现在选中底色是实色 `bg-miniso-red-tint`，未选中是 `bg-white`，表头 `bg-navy-tint`，
一律不带透明度；同一个底色要同时打在首列和四个字段格上，不然固定列会露白。

## 日期跳转必须是点击直接触发

只把跳转挂在 `useEffect([activeDate])` 上会漏一种最常见的操作：手动滑走之后
**再点一次同一个已选中的日期**——state 没变，effect 不重跑，页面纹丝不动。
所以抽了一个 `jumpToDate(date)`，点击时直接调用；effect 只负责在
**切人 / 切语言**之后把选中日期重新带回视野（行高和排列方向都变了），
顺便把横向滚动归零回到「住宿」列（RTL 下 `scrollLeft = 0` 同样是行首）。

不做「手动滚动实时反推当前日期」——那会让 effect 和用户滚动互相触发。
选中只是跳转目标，不影响任何一行是否显示。

同理，日期条自己那个「让选中项可见」的 effect 必须依赖 `lang` 和 `dates`：
切语言后按钮宽度和排列方向都变，浏览器会把 `scrollLeft` 重置到开头，
选中的那天就跑出视野了（真机 QA：中文选 9/25 切阿语，导航跳回 9/20 附近，表格还停在 25）。

## 已知可以再做的

- 没有 URL deep-link：语言 / 人 / 日期都只在组件 state 里，刷新回到默认。
- 9/29–10/5 已经补进日期导航，每天只有 Li/Qiuting 的个人行程行（无广州攻略入口），
  免得这七天从完整行程里漏掉。她的离穗日期仍是待定，页面不推断她已经到家。
- `components/trip/ui.tsx` 的 `CopyButton` 目前没有调用方（矩阵里复制统一在 Sheet 用
  `CopyChinese`）。留着不碍事，要清理可以删。

## 验证

### 事实层（仍然有效）

2026-09-10 owner 核过：全部 8 段航班对照原始机票；18 天 / 45 条人员日记录、分房关系、
会议与学习日期、机场提前 3 小时时刻、对接人。**这一轮没有改
`lib/trip-data.ts` 和 `lib/person-day-plan.ts`，所以这些事实结论仍然成立。**

### 展示层（历史，只记录不当作本轮结论）

上一轮 owner 对**每人一张四行小表**做过 390px 中英阿视口、筛选、航班展开、复制按钮的检查。
那套 UI 已经被这次的横向字段表替代，**上述展示层检查不适用于当前实现**。

### 本轮

实现交接时尚未验收；随后 owner 已完成本文开头所列手机浏览器验证，TypeScript、ESLint及Sites正式构建均通过。事实两文件与基线逐字相同。
