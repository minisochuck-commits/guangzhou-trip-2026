# 设计记录 · 广州行程分享页

产品定位：**一张精致好用的行程单**，不是营销落地页。手机优先，看的人是 5 位出差的同事，
不是潜在客户。所以没有大 hero、没有视差、没有 CTA。

## 配色

MINISO 品牌红 + 深海军蓝 + 白底，定义在 `app/globals.css`：

| 变量 | 值 | 用在哪 |
| --- | --- | --- |
| `--miniso-red` | `#e4002b` | 品牌标识条、「待确认」、关键缺口、官方链接 |
| `--miniso-red-strong` | `#b8001f` | 红底上的深色文字、链接文字 |
| `--miniso-red-tint` | `#fff1f3` | 提示条底色、「待确认」块底色 |
| `--navy` | `#0e2240` | 正文主色、「已确定」实底、日期按钮选中态 |
| `--navy-soft` | `#33496b` | 次级说明文字 |
| `--navy-tint` | `#eef2f8` | 航班块、人名 chip、分类 chip 的底 |
| `--line` | `#dfe4ec` | 卡片描边 |

红色**只用于强调**（提示条、待确认、关键缺口、链接），大面积文本一律海军蓝，底始终是白。

**页面固定浅色**：删掉了 starter 的 `prefers-color-scheme: dark` 分支，并设
`color-scheme: light`。理由是需求明确要求白底行程单；跟随系统进深色会让「白底 + 红蓝」
这套关系失效，而这份文档没有夜间长时间阅读的场景。

## 状态三态

`confirmed` / `suggested` / `pending`，在 `components/trip/ui.tsx` 的 `StatusPill` 里
映射成三种视觉，刻意做得能一眼分开：

- **已确定** = 海军蓝实底白字（最"重"，代表事实）
- **建议安排** = 白底 + 虚线描边（视觉上"没落地"，对应"没预订"）
- **待确认** = 浅红底红字（要人去处理）

虚线是有意的：建议项看起来就不该像一个已经敲定的方块。

## 排版与可读性

- `html { font-size: 16px }`，正文 `text-base`（16px）。
- 页面上任何可见文字**不低于 14px**（`text-sm`）；浏览器实测最小值就是 14px。
- 阿语行高放大到 1.85（`[dir="rtl"]`），因为阿拉伯字母上下延伸多，1.65 会挤。
- 内容宽度 `max-w-[42rem]` 居中，手机上就是满宽。

## RTL

- 语言切换时用 effect 同步 `document.documentElement.lang/dir`，并把整个 `trip-root`
  包在 `dir` + Radix `DirectionProvider` 里（Radix Tabs 的方向键行为需要它）。
- 布局一律用**逻辑属性**：`ps-*` / `ms-auto` / `before:start-0`，不用 `pl-*` / `ml-auto`，
  这样镜像是自动的。
- 航班块中间的箭头用 `rtl:rotate-180`，外链图标用 `rtl:-scale-x-100`。
- **姓名、航班号、时间、IATA 代码、URL 一律包 `.ltr-token`**
  （`direction: ltr; unicode-bidi: isolate`）。`isolate` 是关键：没有它，`3U6701` 后面
  跟阿拉伯文时标点会被 bidi 算法甩到错误的一侧。
- 浏览器实测：29 个 `.ltr-token` 在阿语下 `direction` 全为 `ltr`。

## 横向不溢出

- `html, body { max-width: 100%; overflow-x: clip; overscroll-behavior-x: none }`。
  `overscroll-behavior-x: none` 是需求点名的，防止手机上左右橡皮筋。
- 需要横滑的三处（语言 chips、人员 chips、日期导航）各自 `overflow-x-auto scrollbar-none`，
  内层 `w-max`，滚动被关在自己的容器里。
- 日期导航用 `-mx-4 px-4`，让它滑到屏幕边缘但不撑破页面。
- 实测 390px 宽下，中 / 英 / 阿三语、三个 tab 的 `scrollWidth` 都等于 `clientWidth`。

## 信息密度上的取舍

1. **参与者 chip 只显示票面短名，不带职务。** 一开始每张卡都写「QIUTING 商品负责人
   HASSAN 防损负责人 …」，9/21 这种一天 8 张卡的日子直接废掉半屏。职务改成在
   日程 tab 顶部的「人员」折叠名单里给一次（带票面全名 + 分组 + 同行说明），
   卡片上的 chip 只留短名，`title` 属性里仍保留全名和职务。
2. **姓名一律用票面拼法**：`LI/QIUTING`、`HASSAN/MOHAMEDMOHAMED`、
   `ABDELMAGEED/AHMED ABED`、`ABDELHAMID/RAHMASAYED`、`EISSA/REHAM MOHAMED`。
   chip 上的短名（`QIUTING`/`HASSAN`/`AHMED`/`RAHMA`/`REHAM`）是票面名字的截取，
   **没有另造拼法**，三语下都一样，不做音译。
3. **不给中文名。** 票面只有拉丁拼写，凭拼音倒推汉字会写错人名，所以不写。
4. **一天之内按真实时间轴排序，不按数组顺序。** 早期版本用数组顺序，结果 9/21 的
   MS958（开罗 00:20）排到了成都 08:00 那班之后，读起来像是它更晚。现在把
   `time` + `tz` 换算成 UTC 毫秒再排（`instantOf`）：开罗 00:20 = UTC 21:20（前一天），
   成都 08:00 = UTC 00:00，顺序自然正确，午夜和跨时区都不会颠倒。
   时区映射写死在 `lib/trip-i18n.ts`：北京全年 UTC+8；埃及 2023 年起恢复夏令时
   （4 月最后一个周五 – 10 月最后一个周四），2026 年的截止日是 10/29，
   本行程 9/20–10/7 全在夏令时内，所以开罗一律 UTC+3。只有两个时区，不引运行时时区库。
5. **没有确切时刻的事件不参与时间排序**，单独归到当天的「时间待定的安排」一段
   （`UI.untimed`，上下加分隔线）。绝不为了排序给它们编一个时刻 —— 学习组报到、
   酒店入住、成都中转的具体时间本来就是待确认的。
6. **跨日事件的每一天都出现在日期导航上。** 早期版本只取起始日，结果接待组
   9/22–26 明明是连续 5 天的阶段，REHAM / RAHMA 的个人视图里却点不到 23、24、26 日。
   现在按 `date`–`endDate` 严格逐日展开（`datesBetween`），同一张「待确认」卡在区间内
   每天都能看到；这不会凭空造出活动，卡片内容和状态没变。
7. **切人时不改写选中日期**：`activeDate` 保持用户选的值，渲染时再算出该人最近的一天
   （`effectiveDate`）。这样从 REHAM 切回「全部」时，日期不会被前一次筛选带跑。

## 事实纪律（这些是产品要求，不要"优化"掉）

- 航班信息**按出票行程单 PDF**，不是实时航班查询；不放 PNR、票号、原始 PDF。
- 建议到场时间标成「建议」，依据是票面的国际 4h / 国内 2h，不写成硬性规定。
- 成都中转写「T1 → T2 需换航站楼」+「行李是否直挂以值机柜台确认为准」，
  **不假定直挂**。
- 9/23 下午的门店巡店是「待确认是否报名」，不能写成已参加。
- 9/24 订货会第三天标 `pending`——总部通知说仅上午是事实，但**她是否参加没定**，
  不替她推定。
- 9/27 晚间送机单独做成高亮卡，这是整份行程里最容易出事的一处。措辞是
  **「晚间送机接待人待确认」**，不是「无人陪同」—— 我们只知道 RAHMA 计划 17:25 起飞
  因而无法陪同 REHAM 23:20 的夜班机，**不知道有没有别人接手**，断言"无人"是造事实。
  同理这里写"陪同接待"而不是"看护"，写"计划起飞"而不是"已经离开"（行程还没发生）。
- 9/28 回开罗是**两批**（T3 04:50 / T2 06:50），页面明确写"不能合并为同一次接机"。
- 酒店没定就不推荐具体酒店、不放导航地址；可复制的中文地址只给已核实的机场航站楼
  和门店所在商业体（并注明街道 / 入口待确认）。
- 行李额只写件数（川航 1 件、埃航 2 件），**票面没标公斤就不猜公斤**。
- 城市体验只用官方来访指南里能核到的北京路、沙面两项；没有票价 / 开放时间就不写。
  傍晚花城广场 / 珠江新城只写"可以走走"，不默认订塔票或游船。
- 来华指南不写"免签适用"，签证状态让本人向使领馆确认。
- 不编造紧急电话、联系人、国内机票或酒店。

## 访问

页面**公开**：无登录、无密码、无 session、无 env、无解锁接口。
（早期版本曾计划做共享口令门禁，用户明确取消，相关设计已整体移除，代码里不留残留。）

## 底部参考图

`public/reference/hq-notice.png` = 总部面向全体的**总通知**原件，折叠在页面底部。
图下三语说明写明「本团实际安排以本页已核定的日程为准」——总通知覆盖的人和场次比本团多，
不能让人拿它当本团行程读。**带个人电话的轮岗申请、原始机票行程单不放上去。**

## 页脚

页脚原来写「本页只呈现已核定的信息」，与页面上大量「建议安排」并存是自相矛盾的。
现在是一句：**「航班按票面当地时间；建议及待确认安排尚未落实。」**
只讲两件读者真正需要知道的事：时间口径，以及哪些还没落地。

## 已知可以再做的

- 没有 URL deep-link：语言 / 人 / 日期都只在组件 state 里，刷新回到默认（中文 / 全部 / 9-20）。
  如果需要把"REHAM 的 9/27"直接发给某个人，需要加 query 参数同步。

## 验证

第一轮：用系统 Chrome + CDP 跑过一轮真实浏览器验收（390×844 移动视口），42 项全过；
`npm run build`、`npm run lint`、`npx tsc --noEmit` 均通过。

第二轮（本次返工）：按 Codex 浏览器验收提出的 7 项修复了跨日日期导航、9/27 送机措辞、
9/27 T3 说明里的 morning/الصباحية 错字、参考图接真图、页脚口径、同日跨时区排序、
`.gitignore`。**这一轮的构建、类型检查和浏览器复验由主窗口统一执行**，我这边没有再跑。
