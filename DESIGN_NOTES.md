# 设计记录 · 广州行程分享页

## 这一轮（吃住行玩 + 个人链接 + 中国风视觉）

用户当次的三件事：**页面重点是让客人在广州吃好住好行得顺玩得好**（不是行程管控、
不是业务安排）、**每人一条自己的链接、另有一条合并的给中国同事统筹**、**页面要好看**。

用户当次明确划掉的，不要再加回来：待定追踪表、区域负责人是否接待、中秋国庆的客流
与巡店价值、机票风险提醒、巡店观察表、订货会问题清单、地铁与公共交通
（**全程打车**）、通讯方式的操心（Rahma 用埃及号码漫游）。原话：「这些关你屁事」
「你干预我业务安排了吧」。**待定就是细节还没出，照实标着即可，不需要收口机制。**

### 1. 内容：礼拜与清真餐（新增一节，排在行李之前）

五人里四位是穆斯林，页面此前只教人「问有没有猪肉」，没给任何一个能去的地方。
新增 `MOSQUES` / `HALAL_DINING` / `HALAL_WHERE` / `JUMUAH_NOTE`（`lib/trip-data.ts`）：

- 三座清真寺按**政府门户**写：清真先贤古墓（越秀区解放北路901号之一，省内最大礼拜殿）、
  怀圣寺光塔寺（越秀区光塔路56号）、濠畔清真寺（越秀区濠畔街378号）。
- **9/25 是这趟在广州唯一的周五**，主麻时间各寺不同，页面只提醒去问，不写具体时刻。
- 清真餐只收一家有据可查的老字号（回民饭店，中山六路325号，1956 年为在穗穆斯林所开）。
  **营业时间与电话来自点评网站，卡片上写明未向店家核实。**
- `HALAL_WHERE` 三条把位置关系说在前面：清真馆子集中在越秀老城，保利洲际在琶洲，
  要打车；酒店能否安排清真餐**由客人自己问酒店，页面不代为承诺**。

新增 `PlaceCard` 类型与 `PlaceCardView`：名字、一句话特点、给司机看的中文地址、
开放信息、来源。**中文地址那行按「举给司机看」的距离排字号**（`CopyChinese` 的
`showBig`），全程打车时这是最常用的动作。

`PREP` 增一条「天气与穿什么」（九月平均值，写明不是预报）；`PHRASES` 增四句打车
和问价用的中文。

### 2. 每人一条链接

`?p=reham&lang=ar` 打开就是本人、本人语言；`?p=all` 是给中国同事统筹的合并视图。
**地址栏是「看谁 / 哪种语言」的唯一真相**（`useSyncExternalStore` + `replaceState`），
不是组件 state —— 这样本人在页面上改了选择，复制当前网址转给别人仍是同一个视图。
`mohamed` 是 `hassan` 的链接别名（代码 id 沿用票面首字段，但发出去的链接要让本人认得）。
服务端快照返回空串，SSR 与首次水合渲染的都是默认视图，不会两边 HTML 对不上。
（先前写成「挂载后 setState」，被 `react-hooks/set-state-in-effect` 拦下，
那条 lint 是对的：URL 是外部状态，该订阅而不是拷进 state。）

### 6. 同一轮追加：分人的出发前准备、文化差异、路线换血

用户三条：

1. **出发前准备里的手机上网 / 翻译与求助 / 支付是给区域经理与防损的，Reham 不该看到。**
   `PrepItem` 加 `audience?: PersonId[]`，那三条标 `["ahmed","hassan"]`；
   Reham 单独两条（`reham-transfer`：MS958 去程 9/20 晚 21:20 到开罗 T3、公司安排接送
   Rahma 对接、9/27 MS959 夜航与行李寄存待确认；`reham-page`：这页怎么用），
   **只写已知适用于 Reham 的事，支付与上网没写 —— 不知道她的安排，不编。**
   天气一条所有人都看。`GuideTab` 按 `person` 筛，`?p=all` 不筛给统筹看全貌。
2. **新增「常见的文化差异」**（`CULTURE_NOTES`，11 条）：周五是工作日与礼拜、
   「清真」很多人不懂、酒桌以茶代酒、不给小费、钱在手机里、握手与称呼、
   有些问题不是冒犯、饭桌规矩、被拍照被看、「再研究一下」常常是「不」、时间就是那个时间。
   口吻是「这里的习惯是这样」，每条给一个当场能用的办法。
3. **路线换血**：用户原话「黄埔古港又穷又破」「沙面还行」「花城广场一定要去」
   「怎么震慑住他们，让他们感觉到广州的科技、现代化与繁华」。
   删黄埔古港（连琶洲段里的一句提及也删了，只留史实）；新增花城广场·新中轴（必去，
   56 公顷 / 东塔 530 米 116 层 / 扎哈·哈迪德的大剧院）、登广州塔、珠江夜游
   （大沙头 / 天字码头）、天河路·太古汇（2.8 公里、240 万㎡、日客流 150 万、
   年销售过万亿、2024 全国商圈第三）；沙面留，标题改「沙面 · 与西关骑楼」。
   门票与营业时间照旧不写未核实数字。
   **踩坑**：旧的第三条路线 id 也叫 `huacheng`，新旧同存时 React key 撞了、页面出现
   两个花城广场 —— 靠浏览器探针（按 h3 列路线）抓到的，已删旧条。

### 5. 再返工：用户说出了这份指南的真正目的

原话：「我们就是想让埃及朋友来看看中国有多强大。他们才好敬仰，才给政策，才认真对待。」
此前所有轮次都按「帮客人吃住行」在做，从没人说过这一层。按这个目的衡量，
上一版确实失败：一个字没提广州的现代化与经济体量，饮食只有五样小吃，没有图。

改法是加两大块、并把它们放在指南最前面、不折叠：

- **这座城有多大**（`CITY_SCALE`）：一句大字「一座城市的经济总量，超过埃及全国」，
  两段正文，八格数字（GDP 3.2 万亿 / 1910 万人 / 白云机场 8359 万人次 / 广州港 6.96 亿吨 /
  地铁 780 公里 / 广州塔 600 米 / 广交会 31 万境外采购商 / 大湾区 15.3 万亿），
  八条来源。**数字全部出自 2025 年官方口径**：广州统计公报、民航局、广州港务局、
  交通运输局、海珠区政府、人民网广交会通报、大湾区门户网；埃及 GDP 取世界银行 2024 年数。
  美元换算只作量级对比并写明汇率假设 —— 3.2 万亿元在 8.2 以内的任何汇率下都大于
  3890 亿美元，结论稳。数字块 `value` 分三语（中文按万 / 亿，英阿按 million），
  `dir="ltr"` 保证阿语下数字不翻。
- **你们住的这块地**（`PAZHOU`）：酒店 828 号、广交会展馆 382 号，同一条阅江中路；
  这块地从海丝出海口 → 广交会（155 万㎡，第 138 届 3.2 万家企业、223 个国家地区）
  → 腾讯阿里唯品会总部所在的试验区（3.6 万家企业、年营收 4500 亿以上）。
- **食在广州**（`FOOD_CULTURE` + 扩到九道菜）：1925 年《广州民国日报》「食在广州一语，
  几无人不知之」；粤菜八大菜系之一、两千年、三大流派；顺德 2014 年联合国教科文组织
  「世界美食之都」（全球六座）；潮汕牛肉火锅当天现宰、一头牛三分之一上桌。
  新增老火靓汤、潮汕牛肉火锅、白切鸡、干炒牛河与「镬气」四道，每道讲它为什么值得。
  原「吃什么 · 文化小知识」并入此节。

配图仍是内联线稿：新增 `CantonTower`（广州塔）与 `LightTower` 并列 ——
一千四百年前的塔和今天的塔。**真正的食物照片没有**：页面零外部请求、离线可用，
放照片只能内嵌；而下载图片文件需要 owner 明确同意（版权与授权），
所以先把版式留好位，把图片来源作为问题交给 owner 定（见对话）。

指南现在的顺序：广州与你们 → 这座城有多大 → 你们住的这块地 → 食在广州 →
礼拜与清真餐 → 出发前准备 → 行李 → 地址 → 短句 → 路线 → 来源。
默认展开「你们住的这块地」「食在广州」。

### 4. 返工：用户看了实物说「这么丑」「还不能折叠」「内容不够好玩」

三条都成立，记下来避免再犯：

**丑** —— 中国风只做在了页头（印章、宋体、木棉），往下正文一点设计都没有，
就是四个一模一样的白盒子配红点。**页头做完不等于页面做完。**
页头下沿那条回纹尤其难看，像贴纸，已删（`.trip-fret` 连样式一起删掉，不留死代码）。
现在正文分三层：开篇故事 `.trip-story`（暖纸底 + 朱红细线，唯一不是白卡的一块）、
八节 `.trip-card`、节内 `.trip-place`（无阴影，避免卡中卡叠两层阴影）。

**不能折叠** —— 指南原来全部摊开，一屏看不完，翻到底都是同一种盒子。
现在八节都是 `AccordionItem`，收起时只剩标题 + 一行提示（`UI.guideHints`），
整份指南就是一张目录；默认只展开「礼拜与清真餐」。
顺带去掉了「吃什么」和「官方来源」里的内层 Accordion —— 外层已经能折叠，
再套一层只是多点一次。

**内容不好玩** —— 这条最该记。前几轮的「证据口径」写法被我套到了本该让人**想去**
的内容上：清真寺写成了政府简介（占地多少、容纳多少人），吃的每一条结尾都重复
「不等于清真」（同一句警告出现三遍，而 `FOOD_ADVICE` 顶上已经统一说过一次）。
**最动人的那件事反而没写**：怀圣寺是公元 627 年阿拉伯商人出钱建的，光塔三十六米，
唐代广州城西有「蕃坊」，开元年间每年八十多万人次外商往来、多数是阿拉伯人 ——
**四位埃及客人来的是一座阿拉伯人参与建起来的城**。
新增 `CITY_STORY` 放在指南开头，不折叠；清真寺与吃的全部改成先讲故事。
宛葛素的身份按传统说法写并注明史家有争议 —— 不拿宗教史当定论，
这跟「不把未核实写成已核实」是同一条纪律。

配图是手画的 `LightTower` 内联 SVG（光塔线稿），不引外部图片，离线照常显示。

### 3. 视觉：宣纸底 + 印章 + 宋体（用户要「高级感的中国风 + MINISO 元素」）

不用灯笼龙纹那一套。落点是四样，全部系统字体与内联图形，**不发外部请求，
离线和大陆都照常显示**：

- **纸色底 + 白卡片**：`--page` 改成 `#efece5`，`.trip-card` 统一卡片长相
  （白底、淡描边、极轻阴影）。此前整页和卡片都是白的、边框都一样，
  所以看着什么都在同一层。九处各写一串 `border border-line bg-white` 已收进一个类。
- **印章**：`.trip-seal` 一枚朱红方章，字是「穗」。中国的印是红方章，MINISO 的标
  也是红方块 —— **同一个形状，一个记号同时说清「这是中国」和「这是 MINISO」**，
  不用摆两套符号。
- **宋体标题**：`--font-display` 拉丁落 Georgia、汉字落宋体、阿语落 Naskh
  （字体回退逐字符，拉丁衬线放前面不抢汉字）。标题、卡片名、标签页用 `.trip-display`。
- **回纹与木棉**：`.trip-fret` 用「回」字（方中套方）作单元收在页头下沿——几何母题
  和伊斯兰纹样同源，两边客人看着都不陌生；`.trip-kapok` 是广州市花木棉，
  页头右下角 7.5% 透明度的水印。两者都是 data URI。
- 标签页从灰底药丸改成**朱红下划线**（`variant="line"`）；每行重复的「餐饮 / 住宿」
  降成小号眉批，让内容自己站出来。

**踩过的坑**：`backdrop-filter` 与 `overflow:hidden` 一起加在 sticky 页头上，
Chromium 会把页头画错位置。裁剪已移到内层普通容器，sticky 那层不再自己裁。
（另：浏览器面板滚动后的截图会有不重绘的假象，用 `elementFromPoint` 命中测试
核实布局，别按截图去改代码。）

表格结构、合并规则、筛选与日期跳转**一律没动**。

2026-09-11 本轮自查：TypeScript、ESLint、`vinext build` 均通过；
860px 与 1280px 下页头、标签页、指南各节、礼拜与清真餐卡片渲染正常；
`?p=reham&lang=ar` 打开为阿语 RTL + Reham，`?p=all` 为合并视图。

## 上一轮（三列表 + 默认看 Reham）

用户拍板的版式改动。**只改展示，两份事实文件一个字没动。**

1. **默认人选是 Reham**，不再是「全部」（`trip-view.tsx` 的 `DEFAULT_PERSON`）。
2. **表格固定三列**：单人 `日期｜活动与交通｜食宿`，全员首列换成 `日期 / 人员`。
   活动与交通合进一格（中间一条细线），餐与住合进一格（各带一个短标签）。
   每一段仍旧由原来的 `PlanCell` 按原字段渲染 —— 弹窗、按内容命名的按钮、待定标记
   全都保留，只是两两放进同一格。单人视图首列不再重复写名字（顶部已经写着在看谁），
   全员视图照旧列名字。**合并规则、筛选、日期跳转都没动。**
3. **390px 三列全放得下，表格因此不再横向滚动。** 首列宽度由 `.trip-matrix` 的
   `--col-date` 定，后两列对半分（见下面「三列怎么分宽」），
   `[data-slot="table-container"]` 的 overflow
   覆盖成 `visible`，整页只剩一个纵向滚动。随之删掉：列头条与表格之间的 `scrollLeft`
   双向同步、筛人后横向归零的 effect、以及「左右滑动查看」那句提示。
4. **桌面放宽到 1200px**（`md:max-w-[75rem]` + `md:px-8`），列跟着容器长，
   不再是写死的 776px，右边不会被切。
5. **删掉「人员详情」与「参考：总部通知」两块** —— `TeamDetails` / `ReferenceBlock`
   连同 render、import 和 `UI.team` / `UI.ticketName` / `UI.reference` 一起删。
   票面全名现在页面上不出现（事实仍在 `lib/trip-data.ts`）；
   `REFERENCE_IMAGES` 留在数据里，暂时没有引用。
6. 窄屏正文 14px、弹窗按钮 13px 且**允许换行**（`h-auto` + `min-h-11`，
   保住 44px 点击高度，不裁字），≥768px 回到 16px / 14px。阿语 RTL 照旧。

2026-09-11 已验收（基线 3f186e0a611080e7814c629b3ed64ab6a4b35052）：
TypeScript、ESLint、Sites 构建通过；8 航段与18天行程深比较一致，13项合并检查通过。
390px 中文、英文、阿文三列无页面横向溢出；默认Reham 9行、全部42行，人员详情和总部通知均不渲染。
1440px下内容容器1200px、表格1134px，右侧完整；手机及电脑列头边界与正文偏差不超过0.5px。
已实际打开航班弹窗、切换人员及语言，并验证日期按钮跳转到目标行。

## 上一轮（内容归位）

用户确认的四条，只改这四条：

1. **完整航程只挂出发日**（9/20 含 Reham、9/27 三组、10/6 Li/Qiuting）。
   抵达日 9/21、9/28、10/7 保留抵达时刻 / 航站楼 / 接机协调，但不再重复同一个航班弹窗
   —— `CellView.hideJourney`。不同人各自的出发日弹窗互不合并。航班与行李数据未动。
2. **餐饮格只写安排与责任**，没有弹窗按钮；日程里不再出现游玩路线与美食建议。
   路线、美食、文化**只在来华指南**（`RouteList` + `FOOD_NOTES` + `FOOD_ADVICE` 仍在那里）。
   真实排好的巡店（study 9/24–25、Reham 9/23 或 9/24 择一）连同交通待定照旧。
   删掉了 `ROUTE_SHORT` / `FOOD_PICK` / `FREE_ROUTES` / `routesFor` 等已无用的建议代码，
   `CellView` 去掉 `suggestion` / `routeIds` / `foodNoteIds`。
3. **总部会议在住宿酒店内**（用户确认）：9/22–24 参会日交通改成「酒店内参会 /
   Meeting inside the hotel / الاجتماع داخل الفندق」。
   这是**事实层的改动**，不只是展示：`person-day-plan.ts` 删掉 `VENUE_TBD`，
   参会日一律 `row("confirmed", HOTEL_VENUE)` —— 状态从 pending 变 confirmed，
   因为它已经定了。Li/Qiuting 9/24 的返乡交通自理细节保留。
   **只限参会**：新增 `OFFSITE_TOUR_TRANSPORT`，Reham 9/23–24 外出巡店往返交通
   仍是 `pending`；study 9/24–25 的 `STUDY_TRANSPORT` 不动。
   Reham 9/22 是「酒店内参会 + 下午自理」一条 confirmed 文本，交通格不再挂弹窗。
4. **Reham 9/21–26 是双人标间**（不是单间），事实层与展示层同步；
   Rahma 9/25–26 公司另订**单间**不变。不写同房的人是谁，也不加任何无关同事信息。

新增回归 `lib/noise.regression.ts` + `scripts/check-noise.mjs`（沿用 esbuild 那套）。
2026-09-11 本轮已验收（源基线 b48f91a3cf5996122445792d090fb03889124164）：
65 项内容回归、28 项航程回归、13 项合并检查、TypeScript、ESLint、Sites 构建通过。
8 段航班与基线深比较一致。浏览器确认餐饮及游玩入口归零、航班入口 13→6；
Reham 六晚双人标间、会议交通与外出交通分别显示；指南三条路线及美食文化保留。
390px 单人筛选 9 行，页面无横向溢出；英文和阿文相应安排已核对。

## 验收状态

完整航程本轮验收（2026-09-10，源基线 227619bff3879d9b89738f34b3c4908a2869dac3）：28/28 航程回归、TypeScript、ESLint、Sites 构建通过；390px 浏览器确认开罗出发完整两段先于行李，9/28 入口显示9/27开始的完整返程，去回程中转分别170/360分钟；中英阿均含明确的抵达/下一段起飞标签。航班事实和每日安排未改。

本次 owner 验收（2026-09-10，源基线 8f2e2e14d7d58278d30f9c1eefdcd7ad170f63ed）：13 项合并回归、TypeScript、ESLint、Sites 构建通过；390px 浏览器确认 9/20 四人合为一行、合并弹窗列出四人、五人筛选各自只显示本人。中英阿行李详情均明确票面件数与官网重量的区别及订单尚未核实。person-day-plan.ts 未改，trip-data.ts 仅调整行李展示结构与来源说明。下文旧轮次验收按各自基线理解。

**表格方向（字段做列头、每行是「某天的某个人」）是用户确认的结构，不再讨论。**

上一轮 owner 在 390px 中英阿真机上验过：45 行 / 18 天、日期跳转、重复点击跳回、
筛人后横向归零、不透明固定首列不透叠、酒店地址与航班行李弹窗、
Reham 9/22 只给黄埔古港与花城广场。事实数据与起点提交逐字一致。

**那一轮同时暴露了这一版要修的实现不足**，都不是方向问题：

- 总表宽 976px；交通格整句原文把 Reham 9/27 那一行撑到 233px，同一行其它格大片空白。
- 表格自己带纵向滚动面板，和页面滚动叠成两层。
- 所有弹窗按钮都叫「详情」，点开之前不知道会看到什么。
- 自由日主表只写「自由安排」，路线和餐饮建议全藏在弹窗里，第一次来的人不知道能去哪。

展示层重做之后 owner 跑了回归 `verify-readable`，抓到两个 FAIL：
Li/Qiuting 9/28 被按日期分支当成「抵达开罗」；纵向滚动时 sticky 首列盖住了日期条和列头。
两条都已修（见下一节），另外按 QA 意见把建议句、9/27 交通摘要、重复的红色「待定」都压短了。

## 这一轮（降噪）

按用户的整页降噪要求，只改展示：

1. 航班弹窗换成紧凑路线（抬头一次 / 到机场一行 / 每段两行 / 中转一条 / 时区脚注一句），
   去掉编号、去掉中转里重复的到达起飞时刻、去掉单列的「抵达」步。
2. 行李额主区三行 + 一句常显待核实，其余进折叠「行李规则与来源」。
3. 弹窗不再重念 `row.text`，去掉与标题重名的小标题，没内容的格子不挂按钮；
   协调事实改由 `CellView.coordination` 显式带回。
4. 主表去掉纯填充行：「不住酒店」（前一行已说在飞机上）、「未指定餐厅」
   （与「用餐时间未明确」同一件事）、出行日机上餐的通用红色「待定」标。
   真实的未定责任、日期、酒店与需要本人去做的事一个没删。

**未验收** —— 只改了源码和文档，没有构建、类型检查、浏览器验证，回归也没跑。
既有 28 项 journey / 13 项 merge 断言的口径没变（`journeySteps` 数据模型保留，
只是渲染方式变了）；`viewSignature` 加入了 `coordination`，合并结果不变。

## 上一轮（完整旅程）

航班弹窗改成完整旅程时间轴，见下面「航班弹窗给的是完整旅程」。
表格列序、合并、筛选与全部行李事实不变。
新增 `lib/journeys.ts`、`lib/journeys.regression.ts`、`scripts/check-journeys.mjs`。

**这一轮 owner 尚未验收** —— 只改了源码和文档，没有构建、类型检查、浏览器验证，
回归脚本也没跑过。

**上一轮已复验。** 2026-09-10，以 b8fc915e291e39da70548b19c8d5543e0c815f50 为基线：TypeScript、ESLint、Sites 构建及 180 个字段展示回归通过；两份事实文件逐字未改。390px 中文、360px 英文、390px 阿语已检查，筛选行数为全部 45、Li/Qiuting 18、其余各 9。表格宽度 976→776px；中文 Reham 9/27 行高 233→159px；页面无横向溢出，表格不再独立纵滚。酒店、航班与行李、自由行路线仍在对应入口中。

## 这一轮（行合并 + 行李口径）

用户提了两条有界修正，只做这两条，不重新设计：

1. **同一天内容逐字一致的人合成一行**（9/20 出发日四个人）——
   新增 `lib/merge-rows.ts` 与 `lib/merge-rows.test.ts`，见「谁合并、谁不合并」。
   全员视图行数由 45 变 42（9/20 合 4 人、9/28 合 3 人）。
2. **行李额改口径**：票面件数 / 官网重量 / 未核实，三件事分开写；
   「开罗出发」抬头只用在真正从 CAI 起飞的航段；拿掉国内 20 kg 对比；
   换成用户指定的两条川航官网来源。见「行李额：票面 / 官网 / 没查过」。

**这一轮 owner 尚未验收。** 只改了源码和文档，没有构建、类型检查、浏览器验证。
上一轮那句「筛选行数为全部 45」是合并之前的数字，复验时按 42 对。

产品定位：**一张精致好用的行程单**，不是营销落地页。手机优先，看的人是 5 位出差的同事。
没有大 hero、没有视差、没有 CTA。

> 展示结构已被替代过几轮。**当前生效的是「三列行程表」**（下一节）。
> 长事件流、独立「吃住行」tab、按组合并的补充卡、全局待确认清单、
> **每人一张四行小表**、以及**四个字段各占一列、要横向滑动的宽表**
> 都是历史方案，不要再当规则参考。
>
> 更正一处旧记录：上一版曾把「每人一张四行小表」写成用户最终拍板 —— 这是错的。
> 用户后来明确说自己当时的行列措辞有误，并拍板改成下面这张表。

## 主结构：一张三列行程表（当前生效）

**每一行仍是「某天的某个人」**，四个字段两两合成两列。所有日期、所有人共用同一张表。

| 首列 | 活动与交通 | 食宿 |
| --- | --- | --- |
| 单人：日 / 月 / 周几；全员：再加人名 | 活动，细线，交通 | 餐（标签），住（标签） |

合的是**显示**不是数据：每一段照旧由 `PlanCell` 按 `card.activity` / `card.transport` /
`card.dining` / `card.lodging` 各自渲染，弹窗与待定标记都在原处。
顺序仍是用户拍板的「先看干什么、怎么走，再看吃和住」。

- **选人只筛行，不转置。** 表的形状永远一样，换人只是少几行。
- **日期按钮是跳转**，不是「只显示这一天」：点一下把窗口滚到那一天的第一行，
  同时更新选中日期（那几行带浅红底）。筛过人之后日期列表只剩他有安排的日子，
  跳过去也一定落在有内容的行上，不会停在表格底部的空白。
- **只有列头条 sticky，没有 sticky 首列** —— 三列都在屏内，不需要横向滑动，
  首列自然一直看得见。
- 全员 45 条记录合并后 42 行，默认的 Reham 视图 9 行；
  全部来自 `cardsForPerson(person, date)`。

### 三列怎么分宽

宽度是**比例**，不是写死的像素，所以 390px 和 1200px 用的是同一份规则
（`app/globals.css` 的 `.trip-matrix`）：

| 变量 | 单人 | 全员 | 阿语 |
| --- | --- | --- | --- |
| `--col-date` | 3.5rem | 4.75rem | 各再加 1rem（星期与月份词更长） |

**只有首列这一个变量**：`<col>` 后两列不写宽度，`table-fixed` 自然把剩余宽度对半分；
列头条用同一个变量 + 两个 `flex-1 min-w-0`，两边逐像素对齐。
一开始给后两列写了 `calc((100% - var(--col-date)) * 0.58)`，
1440px 真机上表头按 58/42 排、表体按 50/50 排 —— **`<col>` 上的 calc(百分比 − 像素)
浏览器不认**，两边错开近 90px。别再往 `<col>` 上写这种宽度。

表格 `table-fixed` + `w-full`，列宽相加正好等于容器，所以**不可能横向溢出**。
390px 单人视图算下来列宽是 56 / 150 / 150 px（页面 px-4 之后可用 356px），
文字区再各减 16px 内边距。正文 14px、按钮 13px 且允许换行，
`break-words` 兜住长词，按钮不设 min-width —— 这三条缺一条就会顶出横向滚动。

## 展示层的两个坑（真机 QA 抓到的）

**一、按日期一刀切会吃掉 Li/Qiuting 的个人行程。**
`lodgingView` / `diningView` / `transportView` 原来把 `date === "2026-09-28"`
当成「全员抵达开罗」，在判断 group 之前就返回了 ——
结果 9/28 那天她被写成「行程结束 / 机上餐 / 06:50 抵开罗 T2」，全是错的：
**她 9/25–10/5 都在中国走个人行程。**
现在这三个函数一进来先跑 `isQiutingPersonal(date, group)`，
她那一段永远优先命中。`activityView` 本来就把 qiuting 分支放在 9/28 之前，没踩到。
改任何按日期的分支之前，先问一句「这一天她在哪」。

**二、sticky 首列会盖到 sticky 控件条上。**
表体的固定首列自己有 `z-10`，日期条 + 列头条也是 `z-10`；同一个层叠上下文里，
DOM 靠后的表体就画在了控件条上面（真机截图：纵向滚到 Reham 9/27 时，
9/26 的首列贴在日期按钮上，列头被盖住）。
修法是给表格容器一个局部层叠上下文：`relative isolate z-0`，
整块表体被压在控件条下面。层级约定：**页头 z-20 > 日期条 + 列头条 z-10 > 表体**。

三列版已经没有 sticky 首列，这个具体症状不会再出现，
但 `relative isolate z-0` 保留 —— 单元格里以后再冒出任何带 z 的东西，
这个约定还是要成立的。

## 事实层 / 展示层分开

| 层 | 文件 | 职责 |
| --- | --- | --- |
| 事实 | `lib/trip-data.ts`、`lib/person-day-plan.ts` | 唯一正本。**这一版没有改动。** |
| 展示 | `lib/plan-presentation.ts` | 把整句原文压成表内能读的 2–4 短行 |

展示层是**按 (日期 × 人员 × 字段) 手写的短句**，不是截断、不是正则剪、也不是
`line-clamp`。理由：交通格必须保留「几点到哪个机场哪个航站楼、谁负责」——
机械截断第一个掉的就是这些。完整原文一个字不改地留在弹窗的「完整说明」里。

例子（Reham 9/27 交通）：表内两行「20:20 到白云 T3」「公司送机 · Rahma 对接」，
完整航班号、起降时刻、待定的车辆与出发时间在弹窗。
住宿则是「保利洲际」「与 Rahma 同房」，未定酒店是「公司另订 · 单住」「酒店名待定」。

（这两条摘要当时是为了把 776px 宽表里的一格压短。现在合成三列之后短句照旧有用，
但**列宽不再写死像素** —— 见上面「三列怎么分宽」。）

## 只有一个纵向滚动条

最早那版把表格容器限了高，页面和表格各一个纵向滚动条，手机上很难用。
接着的一版改成「页面负责纵向、表格负责横向」，仍然是两个滚动方向两个容器。
**现在三列全在屏内，横向那个也去掉了：整页只有一个纵向滚动。**

要注意 shadcn `<Table>` 自带的 `[data-slot="table-container"]` 有 `overflow-x: auto`，
按 CSS 规则 `overflow-y` 也会跟着变成 `auto`，于是它成了最近的 scrollport ——
表里的真 `<thead>` 没法相对视口 sticky。

做法（不改 vendored 组件）：

- 从外层 `.trip-matrix` 把容器的 `overflow` 覆盖成 `visible`，边框和圆角一并挪到
  这条规则里补回来。它不再是 scrollport，页面就只剩一个滚动条。
- 真 `<thead>` 留在 DOM 里、加 `sr-only`，语义和 `scope="col"` 关联给读屏用。
- 可见的列头是表格**外面**那条 `aria-hidden` 的 flex 三格，
  和 `<colgroup>` 读同一组 CSS 变量，所以**不需要再同步 `scrollLeft`** ——
  以前那把 `syncing` 锁和 `requestAnimationFrame` 防抖已经删掉。
- 日期条和列头条一起包在一个 sticky 容器里，`top` 取
  `var(--trip-header-h)` —— 页头高度由 `trip-view.tsx` 用 `ResizeObserver` 量出来
  写进 CSS 变量，语言换行导致页头变高也不会错位。
- 日期跳转改成 `window.scrollBy`，偏移量按 sticky 容器**当时的实际底边**算，
  所以目标行不会被页头和列头盖住；由点击直接调用，重复点同一天也有效。

### 谁合并、谁不合并

数据层面 Ahmed 与 Mohamed 本来就是一条记录。**展示层再做一次合并**
（`lib/merge-rows.ts`）：同一天里内容逐字一致的人合成一行。

判定标准只有一条 —— **四个字段连同完整细节都一样**。`cardSignature()` 把两层内容
规范化成一个字符串再比：

1. 事实层 `Row`：`status` / `text` / `detail` / `flights` / `copy`
2. 展示层 `CellView`：`lines` / `entry` / `suggestion` / `hidePending` /
   `routeIds` / `foodNoteIds`

**展示层也必须比**：`cellView` 是按 group 分支的，事实一样而展示不一样的两行合并会骗人。
L10n 逐个键取 `[zh, en, ar]`，不依赖对象字面量的键顺序（不同常量的书写顺序可能不同）。

结果：

- **9/20 合并** —— Li/Qiuting · Rahma · Ahmed · Mohamed 当天住宿、活动、餐饮、交通
  （连同 `DIET_ASK`、成都中转说明、`3U3864` 航班）逐字相同。Reham 活动与交通不同，单独一行。
- **9/28 合并** —— Rahma 与学习组同一班 3U3863、同样的抵达时刻与中转说明。
  Li/Qiuting 在中国、Reham 走 MS959，各自一行。
- **其余各天不合并** —— 9/21 室友不同，9/25 一个在中国一个巡店一个自由，
  9/27 Rahma 向董事长确认、学习组向 Rahma 确认。**摘要看着像不算数。**

**没有任何按日期写死的例外。** 想让某天合并，就得让那天的内容真的一致。

筛单人时，`displayedPeople(row, person)` 让首列**只写选中的那个名字**，
不把合并行里同行的人带出来；全员视图才列全。
**室友关系仍然由住宿正文体现**，不靠首列表达。

自检在 `lib/merge-rows.test.ts`（合并 / 不合并 / 筛人 / 合并行代表卡与成员一致）。

**Rahma 和 Reham 不是「接待组」**。早期版本把她们当成全程同吃同住的一组，是错的：
Rahma 9/25 换酒店、9/27 白天走；Reham 一直住洲际、9/27 深夜才走。合并会直接写错事实。

### 航班弹窗给的是完整旅程，不是挂着的那一段

以前弹窗直接渲染 Row 上的 `flights` id：9/20 只挂了 `3U3864`，出发日就看不到成都之后
还有一段；中转说明又是一句「去程 T1→T2、回程 T2→T1」混在一起的抽象文字，
读的人得自己分辨今天算哪一边。

现在 `lib/journeys.ts` 用 `JOURNEYS` 把航段 id 显式归到 5 条旅程，
`journeysForFlights()` 从**任意一段**反查整条。弹窗渲染成紧凑路线（不编号）：

```
开罗 → 成都天府中转 → 广州白云 ｜ 2 段航班
到机场  9/20  11:45  开罗 CAI T2
┌ 3U3864  经济舱  次日抵达
│ 出发  9/20  14:45  开罗 CAI T2
└ 抵达  9/21  05:10  成都天府 TFU T1
  成都天府中转 · 2 小时 50 分 · T1 → T2
┌ 3U6701  经济舱
│ 出发  9/21  08:00  成都天府 TFU T2
└ 抵达  9/21  10:25  广州白云 CAN T2
时间均为机场当地时间
```

第一版做过编号五步（到机场 / 段 / 中转 / 段 / 抵达），中转格里还把到达和起飞时刻
再写一遍、末尾另起一个「抵达」步。信息没错但重复三遍，390px 上很吵。
现在：**抬头一次、到机场一行、每段两行、中转一条、时区脚注一句**，
末段的「抵达」行就是终点，不再单列。

几条硬规矩：

- **中转的航站楼与停留时长是算出来的**，不是另写一份文案 ——
  取相邻两段的票面时刻做差（同一机场同时区，不需要换算）。回程 T2→T1 停 6 小时且
  **跨零点**，所以到达和起飞两侧的日期都写出来，并标「跨零点」。
- **航段 id 区分同航班号的不同日期**：`3U6704-0927` 与 `3U6704-1006` 是两条旅程，
  Li/Qiuting 的回程不会串到 9/27 那套。
- **Reham 是直飞，旅程里只有一段**，绝不能被拼上川航航段；抬头写「直飞」。
- 机场城市在前、三字码在后，航站楼始终写出来，每个时刻都带日期和时区标注。
- `CHENGDU_TRANSFER` 那段混写说明**从弹窗展示中过滤掉**（`plan-cell.tsx` 按引用比对），
  事实正本 `person-day-plan.ts` 一个字没动。当天特有的接送 / 对接事实
  （落地接送待确认、Reham 公司接送对接 Rahma、Li/Qiuting 返穗自理）全部保留，
  排在路线**下面**，待定状态不变。
- 弹窗里**整条路线排最前**，然后才是当天的协调事项。

### 弹窗只给表里没有的东西

以前每个弹窗都先把 `row.text` 原样念一遍，而表里已经是它的短句版本 ——
再加上「完整说明」「航班与行李」这些和标题栏重名的小标题，点开满屏都是重复。

现在：

- **不再渲染 `row.text`**，也不再有「完整说明」「航班与行李」小标题（标题栏已经写着了）。
- 只有真正表里没有的内容才进弹窗：完整路线、行李额、当天的协调事项、
  可复制中文地址、路线、美食与来源。
- **没有这些内容的格子直接不挂按钮**（`hasSheetContent`）——
  点开只念一遍摘要的按钮就是噪音。
- 表里被压掉的协调事实靠 `CellView.coordination` 显式带回弹窗，
  9/27 三行（Reham 公司送机 + Rahma 协调、Rahma 与董事长确认、学习组向 Rahma 确认）
  就是这么保住的；**到机场时刻不重复**，它已经在路线里。
- 行李额一条旅程一段，放在路线之后。主区只留三行：承运人舱位（就是标题，不再另加
  「免费行李额」通用抬头）、票面件数、官网重量，加一句常显的
  `BAGGAGE_SHORT_CAVEAT`「重量按官网标准，订单待核实」——**不确定性不藏**。
  尺寸、完整 caveat、查阅日期与来源链接全部收进一个折叠「行李规则与来源」。
  埃及航空没有 caveat，折叠里给 `BAGGAGE_VERIFIED`，规则原样不变。
  **没有新增任何直挂或中转手续的说法。**

回归在 `lib/journeys.regression.ts`，用 `node scripts/check-journeys.mjs` 跑
（esbuild 打包后执行，不新增依赖）。

### 弹窗入口按内容命名，不叫「详情」

列宽只有 10.5rem。航班块、完整中文地址、三条路线在格子里就地展开会把整行撑到满屏，
所以完整内容一律进底部 Sheet（复用已有组件）。

但按钮**不能都叫「详情」**——点开之前得知道会看到什么。现在按内容取名：
`酒店地址` / `住宿说明` / `航班与行李` / `巡店安排` / `会议安排` / `用餐说明` /
`游玩路线` / `接送安排` / `当天说明`（三语都在 `UI.entries`）。
按钮文字就是弹窗标题，对得上。

**没有额外内容的格子不挂按钮。** 比如「本次行程结束」「市内出行自理」这种，
短句已经是全部信息，挂个按钮点进去只会看到同一句话。
反过来，只要表内短句比原文少了信息，就一定有入口 —— 弹窗第一段是「完整说明」，
原样给回未删减的那一句。

Sheet 是通过 Portal 挂到 `<body>` 的，**拿不到表格那边的 `dir` / `lang`**，
所以 `SheetContent` 上显式写了一遍，否则阿语内容会按 LTR 排。
自带的关闭按钮只有 16px、纯英文、还压在标题右上角，已经 `showCloseButton={false}` 关掉，
换成一个 ≥44px、带中英阿文案的 `SheetClose`；标题区 `min-w-0 flex-1` 给它让位。
弹窗表头吸顶，航班和路线展开后关闭按钮不会滚出去。

### 建议归指南，日程只写安排

> 历史：曾把「可选：西关老城」「可选：肠粉」这类建议放进日程的活动格和餐饮格，
> 还给每个自由日挂「游玩路线」「用餐说明」弹窗。**用户否定了这个做法**，已全部撤掉。

现在的分界很硬：

- **日程表只回答「当天安排是什么、谁负责、定了没有」**。自由日就写「自由安排」，
  餐饮格就写「公司安排 / 自理 / 用餐时间未明确」。没有建议、没有弹窗按钮。
- **游玩路线、美食与文化只在来华指南**：`RouteList`（三条已核实路线）、
  `FOOD_NOTES`（美食文化 + 官方来源）、`FOOD_ADVICE`（清真 / 过敏 / 食材、
  机上特殊餐申请，整站只说一次）。
- **真实排好的巡店不是建议**，照旧留在日程里：study 9/24–25 与 Reham 9/23 或 9/24
  择一，连同各自的交通待定和「哪天待 Rahma 确认」。

## 状态只有两档，而且只标「待定」

`confirmed`（已定）/ `pending`（待定）。早期的三档里「建议安排」和「待确认」在实际内容里
分不清，反而让人以为「建议」比「待确认」更接近落实。现在只问一句：**这件事定了没有。**

**已定的不挂徽章。** 四行里每行都挂一个深色「已确定」既抢走内容列的宽度，又是纯噪音 ——
已定就是默认状态，看正文就行。只有 `pending` 才在**摘要下面**单独起一行显示「待定」，
不跟正文左右分列（阿语的待定文案更长，分列会把内容挤成一条竖线）。

没定的一律 `pending`，并且在正文里写清**待定的是什么**（是酒店名字没定，还是出发时间没定）。

## 联系谁，写在摘要里

交通行的摘要必须同时给出**到机场时间**和**找谁对接**，不能把人名藏进折叠：
Rahma 向董事长确认、Ahmed / Mohamed 向 Rahma 确认、Reham 由公司送机且 Rahma 提前协调。
只有车辆和出发时间这类还没落实的细节才进「详情」。

其余四人 9/21 落地后的市内接送**没有指定负责人**，就照实写「待确认」，不臆测谁去接。

## 姓名

正文统一正常大小写：Li/Qiuting、Rahma、Ahmed、Mohamed、Reham。
全大写的票面姓名读起来像喊人，且在阿语版里更难辨认，所以**只留在「人员详情」折叠里**。
航班号和机场码保持大写，那是标识符不是名字。

代码里 Mohamed 的 id 仍是 `hassan`（票面首字段）。改 id 会牵动所有 `people` 数组，
收益只是好看，风险是漏改，所以不改。

## 事实纪律（产品要求，不要"优化"掉）

- 航班的日期、时刻、航站楼**一律按票面**，任何情况下不改。
- 机场提前到达统一 3 小时（用户拍板），**成都中转不再套 3 小时** —— 中转是衔接，不是重新到机场。
  MS958 跨零点，出发去机场是 9/20 当晚，这一条单独写死，不走模板拼接。
- 行李额写具体数字和来源，但**票面确认的和官网推出来的必须分开说**（见下一节）。
- 酒店名称未定就**不给地址、不给导航**。换酒店后明确提示重算出行时间。
- Reham 的巡店场次在 9/23 与 9/24 之间**择一**，两天都不能写成确定参加。
- Ahmed / Mohamed 的巡店学习**没有固定门店、没有固定课时**。此前写的「黄埔云汇天地」
  只是原申请里的参考，不是已确认的地点，页面上已经拿掉。
- Reham 不是全程公司包餐：会议餐由公司安排，**外出自由活动时可以不参加**。
- 9/21 迎接当天的用餐时间没给，就写没给。
- Li/Qiuting 的离穗日期与所在城市没给，页面**不推断她已经到家**。
- 不保留「谁接手 Reham 未确认」这类旧警报 —— 现在明确了由公司安排、对接 Rahma。
- 出发前准备一律写成「准备 / 测试」，不写「已安装、已充值、已实名」。
  ChatGPT 在中国大陆不在官方支持范围，装了 VPN 也不保证可用，所以同时给离线翻译和
  可复制中文短句作为备选 —— 一句说明，不训人。

## 行李额：票面 / 官网 / 没查过，三件事分开

上一版把「1 件」和「23 公斤」混在同一句里，读起来像两个都已经确认过 —— 不是。
**件数是我们在票面上真看到的；重量和尺寸是从航司官网标准推出来的；
这张订单的重量额度我们没查过。**

`BaggageInfo` 现在按这三层分字段：

| 字段 | 内容 | 页面权重 |
| --- | --- | --- |
| `opening` | 「票面含 1 件免费托运行李」 | 主行，加粗 |
| `openingFromCairo` | 「开罗出发：票面含 1 件免费托运行李」 | 只在从 CAI 起飞的航段替换主行 |
| `lines` | 「官网标准：该件最多 23 公斤；另可随身带 1 件最多 8 公斤」+ 尺寸 | 次要行 |
| `caveat` | 「具体订单的重量额度尚未向川航或出票方核实。」 | 小字 |

三条硬规矩：

1. **不得声称查过航司订座后台。** 我们只看了票面和公开官网。
2. **「开罗出发」不能当通用抬头。** `FlightDetails` 按
   `flight.from.iata === "CAI"` 决定用哪个 `opening`；回程（CAN→TFU→CAI）和指南页
   一律用通用说法。这是上一版的实际错误：回程也顶着开罗抬头。
3. **国内 20 kg 的对比从显眼位置拿掉。** 它是干扰项，而且我们本来就没确认
   这张票适用哪一套规则，拿它当论据反而更像已核实。

来源换成用户指定的两条川航官网页（托运行李页 + 免费行李额说明页）。
**2021 年那份开罗公告针对 3U8392，是另一个航班号，不能拿来证明 2026 年的 3U3864**，
页面不引用。

埃及航空（Reham 两段）是另一家航司，**内容一个字没动**，只是跟着新结构换了字段名。

## 自由行建议的来源

三条半日路线（黄埔古港 / 西关老城 / 花城广场夜景），全部来自已核实的官方来源，
**未预订**，时长写成「规划参考」，不写未核实的票价和营业时间。
`RouteList` 接一个可选的 `ids` 过滤参数，只有一份路线数据，不复制第二份。

**只在指南页出现**，日程表不再按天挑路线（见上面「建议归指南，日程只写安排」）。
`RouteList` 的 `ids` 过滤参数保留，目前指南页不传，给的是三条全部。

## 图片

只保留 `public/reference/hq-notice.png`（总部总通知原件，折叠在页尾），
说明写明「本团实际安排以本页每天的表格为准」。研究里的官方景点照片**未取得再使用许可**，
所以不放；路线用文字表达就够。

## 排版与 RTL

- `html { font-size: 16px }`，正文 16px，标签不低于 14px，可点区域不低于 44px。
- 逻辑属性（`ps-*` / `ms-auto` / `before:start-0`），RTL 自动镜像；箭头 `rtl:rotate-180`。
- 航班号、机场码、时间、姓名包 `.ltr-token`（`direction: ltr; unicode-bidi: isolate`）。
- 日期导航只滚横向容器（`scrollBy` + 可视坐标差），不用 `scrollIntoView`，
  否则会连带滚动窗口纵向，把当天标题顶到 sticky header 后面。
- `html, body` 保留 `overscroll-behavior-x: none` 与 `overflow-x: clip`。

## 表格类名要显式覆盖 vendored 默认值

公共 `Table` 组件给 `<td>` / `<th>` 默认加了 `whitespace-nowrap`、`h-10`、`text-left`、
`align-middle`，直接用会让单元格不换行、高度被压。矩阵里显式覆盖：
`whitespace-normal break-words`、`h-auto`、`text-start`（RTL 才对）、`align-top`。

`<Table>` 本身加 `border-separate border-spacing-0` —— Tailwind preflight 把 `table`
设成了 `border-collapse: collapse`，而 collapse 下 **sticky 单元格的边框不会渲染**，
所以边框全部画在单元格上（`border-b` / `border-e`）。表宽用 `w-auto` 而不是 `w-full`，
列宽用 CSS 变量并同时给 `w / min-w / max-w`：首列 `--col-first` 6.5rem（104px），
四个字段列 `--col-field` 各 10.5rem（168px），总宽 776px。
三个尺寸都给，是为了让表格和外面那条列头条**宽度完全一致** ——
只给 `min-w` 的话，某个长词把一列撑宽，横滑时列头就和内容对不上了。

sticky 单元格必须自带**不透明**背景色。真机 QA 踩过一次：选中日期那几行原来用
`bg-miniso-red-tint/50`（半透明），横滑之后活动/餐饮列直接从固定首列的姓名底下透出来。
现在选中底色是实色 `bg-miniso-red-tint`，未选中是 `bg-white`，表头 `bg-navy-tint`，
一律不带透明度；同一个底色要同时打在首列和四个字段格上，不然固定列会露白。

## 日期跳转必须是点击直接触发

只把跳转挂在 `useEffect([activeDate])` 上会漏一种最常见的操作：手动滑走之后
**再点一次同一个已选中的日期**——state 没变，effect 不重跑，页面纹丝不动。
所以抽了一个 `jumpToDate(date)`，点击时直接调用；effect 只负责在
**切人 / 切语言**之后把选中日期重新带回视野（行高和排列方向都变了），
顺便把横向滚动归零回到「住宿」列（RTL 下 `scrollLeft = 0` 同样是行首，
表格和列头条两个容器都要归零）。

`jumpToDate` 现在滚的是**窗口**（`window.scrollBy`），偏移量按 sticky 容器
`getBoundingClientRect().bottom` 现算 —— 页头高度会随语言和换行变，写死会算错。

不做「手动滚动实时反推当前日期」——那会让 effect 和用户滚动互相触发。
选中只是跳转目标，不影响任何一行是否显示。

同理，日期条自己那个「让选中项可见」的 effect 必须依赖 `lang` 和 `dates`：
切语言后按钮宽度和排列方向都变，浏览器会把 `scrollLeft` 重置到开头，
选中的那天就跑出视野了（真机 QA：中文选 9/25 切阿语，导航跳回 9/20 附近，表格还停在 25）。

## 已知可以再做的

- 没有 URL deep-link：语言 / 人 / 日期都只在组件 state 里，刷新回到默认。
- 9/29–10/5 已经补进日期导航，每天只有 Li/Qiuting 的个人行程行（无广州攻略入口），
  免得这七天从完整行程里漏掉。她的离穗日期仍是待定，页面不推断她已经到家。
- `components/trip/ui.tsx` 的 `CopyButton` 目前没有调用方（矩阵里复制统一在 Sheet 用
  `CopyChinese`）。留着不碍事，要清理可以删。

## 验证

### 事实层（仍然有效）

2026-09-10 owner 核过：全部 8 段航班对照原始机票；18 天 / 45 条人员日记录、分房关系、
会议与学习日期、机场提前 3 小时时刻、对接人。当前精简轮修改了这两个文件的展示文案及导出，
不能以文件未改作为事实依据；已与基线 `3e61cc96e7613351711a6ea14feb193016c247b1`
逐项深比较：8 段航班和 18 天行程数据相同，13 项合并检查通过。

### 展示层（历史，只记录不当作本轮结论）

- 更早一轮 owner 验过「每人一张四行小表」—— 那套 UI 已被横向字段表替代，不适用。
- 上一轮 owner 验过横向字段表的**方向、行数、日期跳转、不透明固定列、弹窗内容**，
  方向结论仍然成立；但那一版的列宽、内纵滚、统称「详情」的按钮、
  只写「自由安排」的自由日格子，都在这一版重做了，**那部分检查不适用于当前实现**。

### 本轮

2026-09-11：精简轮已通过 TypeScript、ESLint、28 项航程检查及 Sites 正式构建。
390px 手机浏览器核对：42 行合并行程，列顺序为日期/人员、活动、交通、餐饮、住宿；
两段航班完整显示，标题与到达时刻不重复；行李来源可展开，订单重量未核实提示保留；
酒店中文地址只显示一次，Reham 送机协调责任保留。
