# 广州行程 Guangzhou Visit · 2026-09-20 – 2026-10-07

MINISO Egypt 一行 5 人赴广州的行程分享页。手机优先，三语（中文 / English / العربية），
公开访问，无登录无密码。

## 从哪儿看起

| 要找什么 | 去哪 |
| --- | --- |
| 对客内容约定、事实来源、本轮改了什么、发布方法 | [`docs/GUEST_GUIDE.md`](docs/GUEST_GUIDE.md) —— **当前正本**，与下文冲突时以它为准 |
| 页面结构、链接口径、口径与检查怎么跑 | 本文件下面几节 |
| 每一版为什么这么改（历史，含已被替代的方案） | [`DESIGN_NOTES.md`](DESIGN_NOTES.md) |
| 站点 starter（vinext / Sites 生命周期、Wrangler） | [`docs/VINEXT_STARTER.md`](docs/VINEXT_STARTER.md) |

README 顶部原来堆着历次「本轮待 owner 验收」的流水账，已经删掉：每一轮做了什么
`DESIGN_NOTES.md` 里都有对应的一节，那记的是**当时**做了什么，不等于现在页面长什么样。

## 页面结构

主体是**一张三列行程表**：每一行是「某天的某个人」，四个字段两两合成两列。
所有日期、所有人共用同一张表。

| 首列 | 活动与交通 | 食宿 |
| --- | --- | --- |
| 单人：日 / 月 / 周几；全员：再加人名 | 活动，一条细线，交通 | 餐（标签），住（标签） |

合的只是**显示**：每一段照旧由 `PlanCell` 按 `activity` / `transport` / `dining` /
`lodging` 各自渲染，弹窗、按钮、待定标记都在原处。
**默认看的是 Reham**（`trip-view.tsx` 的 `DEFAULT_PERSON`），不是全部。

- **只有一个纵向滚动条**：三列在 390px 上全放得下，**表格不横向滚动**。
  页面**不**用 `overflow-x` 把溢出裁掉 —— 真撑出去要在那一处修好，不能靠裁剪掩盖。
  首列宽度是 `app/globals.css` 的 `--col-date`（3.5rem，全员 4.75rem，阿语各再加 1.25rem），
  后两列不写宽度、由 `table-fixed` 对半分；列头条用同一个变量 + 两个 `flex-1` 对齐。
  日期条 + 列头一起 sticky 在页头下面（页头高度由 `ResizeObserver` 量出来写进
  `--trip-header-h`）；可见列头是表格外面那条 bar，和 `<colgroup>` 读同一组变量，
  表里的真 `<thead>` 保留 `sr-only` 给读屏。
- **日期按钮是跳转**：点一下把窗口滚到那一天的第一行并把它标成选中，
  不是「只显示这一天」。重复点同一个已选中的日期也会跳回去 ——
  跳转是点击直接触发的，不依赖 state 变化。选中只是跳转目标，不影响任何一行是否显示。
- **选人只筛行，不转置**。筛完之后日期列表只剩他有安排的日子，跳过去一定落在有内容的行。
  筛了人之后首列**只写他一个名字**，不会把同行的人带出来。
- **同一天安排完全一样的人合成一行**（9/20 出发日：Li/Qiuting · Rahma · Ahmed · Mohamed）。
  判定看的是四个字段的**完整内容**（含弹窗细节、待定状态、地址、航班、路线、餐饮），
  摘要看着像不算数 —— 室友不同、对接人不同、课表不同都不合并。见 `lib/merge-rows.ts`。
- 格子里是**2–4 行短摘要 + 待定提示 + 可选建议 + 一个按内容命名的按钮**
  （酒店地址 / 航班与行李 / 巡店安排 / 接送安排 …；餐饮格与自由日不挂按钮，
  游玩路线与美食只在来华指南）。
  底部 Sheet **只给表里没有的东西**：完整路线、行李额、当天协调事项、
  中文地址、路线、美食与来源 —— 不再把表里那句话重念一遍。
  **没有额外内容的格子不挂按钮。**
- 表里正文 14px、日号 17px、月与周几 13px，**桌面同样不放大** —— 表就是表，
  要读的长句在底部 Sheet 里（15px）。按钮**允许换行**（`h-auto` + `min-h-11` 保住
  44px 点击高度）。分隔线用 `--card-line` 那一档；桌面容器放宽到 1200px，
  列跟着长，右边不会被切。

顶部（sticky，两行）：印章 + 标题 + 日期区间；下面一行是**人员下拉**（默认 Reham，
点开才有其他人和「全部」）与语言切换，都是 44px 高。
**六个人的横滑按钮、木棉水印和页头投影已被替代**，当前的人和日期区间各只写一次。

| Tab | 内容 |
| --- | --- |
| 全部行程 Itinerary | 日期跳转条 → 三列行程表（18 天 · 45 条记录合并后 42 行；默认 Reham 视图 9 行） |
| 来华指南 | 顶上是**默认可见的欢迎开篇**（珠江城市全景 `skyline.jpg`，按 1200×492 自然比例显示、不裁建筑；加「欢迎来到广州」和两句话，不折叠），下面按一次旅行读下来的顺序排：出发前准备 → 酒店与联系 → 机场中文地址 → 琶洲 → 广州与你们 → 这座城有多大 → 科技就在身边 → 礼拜与清真餐 → 食在广州（含 21 个特色餐饮品牌推荐）→ 商圈 → 半日路线 → 文化差异 → 中文短句 → 免费行李额 → 官方来源。一章一张卡，默认全部收起，点开整章连着读完。**没有分组标题、没有勾选清单、没有目录式导航** |

页尾：一行页脚。**没有「人员详情」，也没有「参考：总部通知」** —— 两块都已删除。

**Ahmed 与 Mohamed 同房同活动，数据里就是一条记录，全部视图合并成一行**；
Li/Qiuting、Rahma、Reham 各自一行。选中单人时首列只写那一个名字，
**室友关系由住宿正文体现**。Rahma 和 Reham 不是全程同行组，各走各的。

查看对象切换只是阅读筛选，不是权限隔离 —— 页面本身公开。

> **「每行是某天的某个人、选人只筛行」是用户确认的结构，不要再改。**
> 变的只是列数：上一版四个字段各占一列、总宽 776px、要横向滑动；
> 再上一版还有 976px 宽表、整句原文塞进格子、表格自己带纵向滚动、
> 按钮统称「详情」。那些都是**实现不足**，已被替代。
> 更早的长事件流、独立「吃住行」tab、每人一张四行小表也是历史方案。
> `DESIGN_NOTES.md` 里记了为什么。

## 每人一条链接

| 链接 | 谁用 |
| --- | --- |
| `?p=reham&lang=ar` | Reham 本人，打开就是阿拉伯语、他自己的行程 |
| `?p=rahma` / `?p=ahmed` / `?p=mohamed` / `?p=qiuting` | 各人自己那份 |
| `?p=all` | 中国同事统筹用，看全员 |

**地址栏是「看谁 / 哪种语言」的唯一真相**（`trip-view.tsx` 的 `useSyncExternalStore`
+ `replaceState`），不是组件 state。所以本人在页面上改了选择，地址栏跟着变，
他复制当前网址转给别人，对方看到的还是同一个视图。
`mohamed` 是 `hassan` 的别名 —— 代码 id 沿用票面首字段，但发出去的链接要让本人认得出自己。
参数缺失或写错时，落回默认：Reham + 中文。

## 姓名口径

正文一律用正常大小写的显示名：**Li/Qiuting、Rahma、Ahmed、Mohamed、Reham**。
票面全名（`LI/QIUTING`、`HASSAN/MOHAMEDMOHAMED` 等）**页面上不再出现** ——
装它的「人员详情」折叠已删除，事实仍留在 `lib/trip-data.ts` 的 `ticketName`。
航班号和机场三字码保持大写（`3U3864`、`MS958`、`CAN`、`T2`）。

代码里 Mohamed 的 id 仍是 `hassan`（沿用票面首字段，避免改 id 引发连锁改动）。

## 状态口径

只有两档，不要再加：

| 标记 | 含义 |
| --- | --- |
| 已定 Set | 已经定下来的事实 —— **页面上不挂徽章**，看正文即可 |
| 待定 To confirm | 还没定，摘要下面单独一行显示「待定」 |

**任何未定的都不能写成已预订 / 已安排。** 正文里要写清待定的**是什么**。

## 已确认的事实

### 住宿

- **9/21–9/24 四晚**：全员住 **广州保利洲际酒店 InterContinental Guangzhou Exhibition Center**，
  广州市海珠区阅江中路828号。Rahma + Li/Qiuting 同一间，Ahmed + Mohamed 同一间，
  Reham **双人标间**（9/21–9/26 全程）。
- **9/25–9/26 两晚**：Reham 继续住洲际双人标间；Rahma 由公司另订酒店**单间**（**名称待定**）；
  Ahmed / Mohamed 由公司另订学习地点附近酒店、两人同一间（**名称待定**）；
  Li/Qiuting 回自己在中国的家，住宿与返乡交通自行安排。
- **9/27**：离境人员不新增一晚酒店。Reham 是夜班机，行李寄存 / 延迟退房**待确认**。
- 酒店名称未定时页面**不给地址、不给导航**；换酒店后出行时间要按新位置重算。

### 活动

- 9/21 抵达报到。
- 9/22–9/24：Li/Qiuting、Rahma 参会（9/24 上午参加订货会）。结束后 Li/Qiuting
  回中国的家过中秋，**离穗日期与城市未提供**，10/6 从广州飞、10/7 抵开罗。
- Ahmed / Mohamed：9/22–9/23 参会；9/24–9/25 由 **MINISO 广州区域负责人**带领巡店学习，
  **课表、路线、门店、接送全部待中国团队确认** —— 不写固定驻某一家店，也没有固定上下课时间；
  9/26 自由活动。
- Reham：9/22 上午参会、下午自由；总部 13:50–17:30 巡店场次在 **9/23 与 9/24 择一**，
  **具体哪天待 Rahma 确认**，两天都不能写成确定参加；其余时段自由，9/25–9/26 自由，9/27 离境。
- Rahma 9/25–9/26 留广州自由安排，9/27 返埃及（不是回中国的家）。

### 餐饮

- Li/Qiuting、Rahma：9/22–9/24 会议期间由公司安排。
- Ahmed / Mohamed：9/21 报到起至 9/23 由公司安排，**9/24 起自行安排**。
- Reham：9/22–9/24 会议餐由公司安排，**外出自由活动时可以不参加**（不是全程包餐）。
- 9/25–9/27 自由期间自行安排。
- 9/21 接机与迎接当天的用餐时间**未明确**，页面不编。

### 交通接待

- **总部会议在住宿酒店内举行（用户确认）**，所以 9/22–9/24 的参会日
  **不需要往返用车**，事实层是 `confirmed`，不是待定项。
  适用：Li/Qiuting 与 Rahma 9/22–24、Ahmed / Mohamed 9/22–23、Reham 9/22 上午。
  Li/Qiuting 9/24 的**返乡交通自理**这条细节仍保留。
- **只有参会算酒店内**。仍然待定的外出交通：
  Reham **9/23 或 9/24** 择一的外出巡店往返交通；
  Ahmed / Mohamed **9/24–9/25** 的巡店交通（向 MINISO 广州区域负责人确认，
  不写自行叫车，也不写公司已包）。
- Reham 9/22 下午自由外出的交通自行安排。
- **Reham 往返广州机场全部由公司安排，对接 Rahma**；Rahma 提前协调公司送机。
  具体车辆与出发时间待定。
- Rahma 9/27 去机场的方式与董事长确定；Ahmed / Mohamed 同日机场交通向 Rahma 确认。
- Li/Qiuting 返乡与返穗交通自行安排。

### 机场提前到达（用户已定：统一 3 小时）

| 航班 | 起飞 | 建议到机场 |
| --- | --- | --- |
| 3U3864 · 9/20 CAI T2 | 14:45 | 11:45 |
| MS958 · 9/21 CAI T3 | 00:20 | **9/20 当晚 21:20** |
| 3U6704 · 9/27 CAN T2 | 17:25 | 14:25 |
| MS959 · 9/27 CAN T3 | 23:20 | 20:20 |
| 3U6704 · 10/6 CAN T2 | 17:25 | 14:25 |

成都中转**不再套 3 小时**，按票面衔接时间。弹窗里的时间轴按方向给出真实的
到达 / 起飞航站楼、两侧日期和停留时长（去程 T1→T2 停 2 小时 50 分；
回程 T2→T1 停 6 小时且跨零点），不再用一段去程回程混写的抽象说明。
所有时间都标了所属机场的当地时区。**航班日期、时刻、航站楼一律按票面，不得改动。**

### 免费行李额（按无额外购买）

**两层口径必须分开写，不能混成一句「已核实」：**

| 层 | 来自哪 | 页面怎么说 |
| --- | --- | --- |
| 件数 | **票面** | 「票面含 1 件免费托运行李」（去程从开罗起飞那段写「开罗出发：…」） |
| 重量 / 尺寸 | **航司官网标准**（2026-09-10 查阅） | 「官网标准：该件最多 23 公斤；另可随身带 1 件最多 8 公斤」 |
| 这张订单的重量额度 | **没查过** | 「具体订单的重量额度尚未向川航或出票方核实。」 |

- **四川航空经济舱（票面 1PC，四人四段）**：官网标准 23 kg / 三边和 ≤158 cm；
  随身 1 件 8 kg / 55×40×20 cm（尺寸放在次要行）。
  **不写「已向航司订座后台核实」这类话** —— 我们没查过。
  也**不再放国内 20 kg 的对比**：那是干扰项，而且我们本来就没确认这张票适用哪套规则。
- **埃及航空商务舱（票面 2PC，Reham 两段）**：托运 2 件、每件 ≤32 kg、每件 ≤158 cm；
  手提 2 件、每件 ≤8 kg、55×40×23 cm。限重按每件算。**这一段不变**（不同航司）。

「开罗出发」这个抬头**只用在真正从 CAI 起飞的航段**（`flight.from.iata === "CAI"`）。
回程和指南页用通用说法，不能一律套开罗。

来源链接在指南页每张行李卡下面（川航两条：官网托运行李页、官网免费行李额说明页）。
2021 年那份开罗公告针对的是 3U8392，**不能拿来证明 2026 年 3U3864 的额度**，页面不引用。

## 数据与代码入口

**事实层和展示层是分开的两件事，别混。**

| 层 | 文件 | 放什么 |
| --- | --- | --- |
| 事实 | `lib/trip-data.ts` | 人员、8 段航班、行李额、酒店、出发前准备、可复制中文、三条路线、美食文化、官方链接、参考图 |
| 事实 | `lib/person-day-plan.ts` | **唯一的业务派生**：`cardsFor(date)` 返回当天的人员记录；`cardsForPerson` 按人筛；`datesForPerson` 给日期跳转条 |
| 展示 | `lib/plan-presentation.ts` | 表内的 2–4 行短摘要、弹窗入口类型、自由日路线选择、餐饮建议 |
| 展示 | `lib/journeys.ts` | 航段 → 完整旅程（到机场 / 各段 / 中转 / 抵达）；中转航站楼与停留时长由票面算出 |
| 展示 | `lib/merge-rows.ts` | 同一天内容逐字一致的人合成一行；`displayedPeople()` 决定首列写谁 |
| 自检 | `lib/merge-rows.test.ts` | 合并 / 不合并 / 筛人的断言，见下面「怎么跑」 |
| 事实 | `lib/dining-brands.ts` | 餐饮品牌推荐：21 个品牌按 6 个主题分组，人均参考、可试的菜与来源；推荐的是品牌不是分店，单位与饮食提示只在导语说一次，边界写在文件开头 |
| 文案 | `lib/trip-i18n.ts` | 界面文案与日期格式化 |
| 排版 | `components/trip/ui.tsx` 的 `GUIDE` | 指南的字号尺度，唯一一处；改字号改这里，别在段落上各写一串 |

组件：`components/trip/`
（`trip-view.tsx` 壳 + 页头（印章 / 人员下拉 / 语言）+ 页头高度测量 + 默认人选 ·
`day-tab.tsx` **三列行程表 + sticky 列头条 + 日期跳转** · `plan-cell.tsx` 单元格摘要与
内容化弹窗 · `flight-details.tsx` 完整旅程时间轴 + 行李额 · `routes.tsx` 半日路线
（折叠态只有路线名与时长，照片在展开里）·
`guide-tab.tsx` 指南（欢迎开篇 + 一列介绍章节，章节顺序见上面的 Tab 表）·
`dining-brands.tsx` 食在广州里的品牌推荐（主题 + 品牌行 + 章尾来源折叠）·
`ui.tsx` 公共件与 `GUIDE` 排版尺度（正文 15px / 章标题 17px / 小标题 15px / 副说明 13px，
**整份指南的字号只在这里定**））。

列宽变量和表格容器样式在 `app/globals.css` 的 `.trip-matrix`（含把容器 overflow
覆盖成 `visible` 那条）—— **不改 vendored 的 `components/ui/table.tsx`**，
原因见 `DESIGN_NOTES.md`。

**以下旧模块已删除或置空，由上述新入口替代：**
`lib/day-plan.ts`、`components/trip/itinerary-tab.tsx`、`components/trip/day-supplements.tsx`、
`components/trip/open-items.tsx`、`components/trip/person-day-table.tsx`（每人四行小表，已作废）。
`day-tab.tsx` 里的 `TeamDetails`、`trip-view.tsx` 里的 `ReferenceBlock` 也已删除；
`trip-data.ts` 的 `REFERENCE_IMAGES` 留在数据里，暂时没有引用。

### 改一天的安排（事实）

改 `lib/person-day-plan.ts` 里对应日期的函数。每条记录的四个字段都是
`row(status, text, { detail, flights, copy })`：

- `text` 是**完整原文**，显示在弹窗的「完整说明」里。
- `flights` 填 `trip-data.ts` 里 `FLIGHTS` 的 `id`，弹窗里会出现「航班与行李」。
  **只挂一段也没关系** —— `lib/journeys.ts` 会把它解析成整条旅程（到机场 → 各航段 →
  中转 → 抵达），所以从去程 9/20 或 9/21 任一天进来看到的都是同一条完整路线。
  新增航段时同时在 `JOURNEYS` 里登记它属于哪条旅程；同航班号不同日期靠航段 id 区分
  （`3U6704-0927` vs `3U6704-1006`）。Reham 的 MS958 / MS959 各自单段，保持直飞。
- `copy` 填 `CopyEntry`，弹窗里会出现完整中文原文 + 复制按钮。
  完整中文也要放 `detail`，三语版本都保留中文原文，复制失败时才能手选给司机看。
- 未定的一律 `"pending"` + 明确写出待定什么。

`DATES` 覆盖 9/20–10/7 全程共 18 天，含 9/29–10/5（那七天只有 Li/Qiuting 一行）。
全部视图 45 条人员日记录，合并后渲染 42 行（9/20 合 4 人一行、9/28 合 3 人一行）。

### 合并规则与自检

`lib/merge-rows.ts` 的 `cardSignature(date, card)` 把两层内容规范化成一个字符串：

1. 事实层 `Row`：`status` / `text` / `detail` / `flights` / `copy`（三语逐个键取值，
   不依赖对象字面量的键顺序）
2. 展示层 `CellView`：`lines` / `entry` / `suggestion` / `hidePending` /
   `routeIds` / `foodNoteIds`

签名相同才合并。**没有任何按日期写死的例外** —— 9/20 能合并是因为那天四个人的内容
真的逐字相同，9/21 不能合并是因为室友不同。

`lib/merge-rows.test.ts` 导出 `runMergeChecks()`，返回 `{ name, ok, detail }[]`，
不依赖任何测试框架。项目里目前没装 runner：

- `npx tsc --noEmit` 会连它一起类型检查，`mergeRows` / `cardsForPerson` 签名一变就报错。
- 要真跑断言，接一个 runner 后调用 `runMergeChecks()`，或在临时脚本里
  `import { mergeChecksPass } from "@/lib/merge-rows.test"` 打印结果。

### 改表里显示什么（展示）

改 `lib/plan-presentation.ts`。`cellView(field, date, card, lang)` 按
(日期 × 人员 × 字段) 返回：

- `lines`：表内 2–4 短行。**手写**，不做截断、不用正则剪、不用 `line-clamp` ——
  交通格必须保留「几点到哪个机场哪个航站楼、谁负责」。
- `entry`：弹窗入口类型（`hotelAddress` / `flights` / `storeVisit` / `meals` /
  `routes` / `transfer` / `sessions` / `stayNote` / `notes`），文案在 `UI.entries`。
  `null` = 这个格子没有额外内容，不挂按钮。
- `suggestion`：表里直接看得见的一条 14px 建议 —— **只一条、只短地名 / 只一样吃的**。
  其余选项和「未预订」说明在弹窗里。
- `hidePending`：短行里已经写清「待定的是什么」时置 true，
  不再另挂一行红色「待定」拉高行高；完整状态在弹窗里仍然显示。
- `routeIds` / `foodNoteIds`：弹窗里展开哪几条已有路线 / 美食条目。

自由日给哪条路线写在同文件的 `FREE_ROUTES`（数组第一条 = 主表露出的那条），
吃什么写在 `FOOD_PICK`，都按 `日期|人员` 定。

⚠️ **按日期分支之前先判 `isQiutingPersonal`**：Li/Qiuting 9/25–10/5 在中国走个人行程，
不能被 9/28「抵达开罗」这类一刀切分支吃掉。

**改短摘要时先对一遍原文，别把关键事实漏掉** ——
交通必须留「几点到哪个机场哪个航站楼 + 谁负责」，住宿必须留「谁订的」。

## 本地命令

```sh
npm run build                 # 构建 Sites 产物
npm start -- --port 8788      # 本地跑构建后的 Worker
npm run dev                   # Vite/Vinext 开发预览
npm run lint                  # eslint
npx tsc --noEmit              # 类型检查

node scripts/check-guest-guide.mjs      # 分人可见性、三语齐全、禁用措辞、指南结构
node scripts/check-journeys.mjs         # 航段接得上、中转时长为正
node scripts/check-noise.mjs            # 弹窗降噪与交通 / 住宿口径
node scripts/check-render.mjs           # 三语 × 六视图服务端渲染冒烟（不起服务）
```

检查器能证明内容没掉、口径没跑，**证明不了好不好看** —— 视觉与交互仍要在真机
390px 上过一遍中文 / English / العربية。

依赖已按 starter 锁定版本装好，不要重装或升级。

## 访问

页面公开：无登录、无密码、无 session。不放 PNR、票号、原始行程单 PDF 或个人电话。
Sites 的注册、发布与生产验收由站点 owner 负责。
