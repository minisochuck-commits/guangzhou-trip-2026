# 广州接待指南：内容与交付

## 餐饮品牌与首图的实际检查（2026-09-11）

本轮基于 `7f190c2d7432e7c062c38579e4c5ab7a444da027`。正式构建在 390px 中、英、阿三语实测：欢迎全景保持完整比例；餐饮显示 21 个品牌、芒果树一条；中文标题不重复检索名，英文与阿语保留中文检索名；三语复制均显示成功。390px 三语与 320px 阿语无横向溢出；1280px 欢迎图为完整全景、左图右文。最终导语压缩后重新构建，并在 390px 中文再次检查排版与复制。

TypeScript、ESLint、指南规则检查及正式构建通过；实施阶段 18 视图渲染、65 项降噪、28 项行程检查通过。`lib/trip-data.ts`、`lib/person-day-plan.ts` 未改。菜名依据 `lib/dining-brands.ts` 每条来源，人均沿用提供的门店参考，不能称所有分店的实时价格；检索来源不等于清真认证。以上是运行与内容核对，不等于用户已经认可视觉设计。最终源码提交由线上 `source-version.json` 对应。

## 欢迎开篇与顺序调整的实际检查（2026-09-11）

本次基于 `6cb0c99`：390px 中文、英文、阿语进入指南即可完整看到欢迎图文，随后是出发前准备，再是酒店信息；三语均无页面横向溢出。准备章节展开后原有说明仍可连续阅读，正文 15px，无勾选项。1280px 欢迎区已实测为左图右文，照片为 320×220px，不再拉成满宽屋檐横幅。章节顺序与本次约定一致，每章一次；浏览器无运行错误。TypeScript、ESLint、指南检查、18 个视图渲染及正式构建通过。事实数据与行程派生文件未改；这是实现验收记录，不代替用户的视觉评价。

## 上轮缩小字号修订的实际检查（6cb0c99，2026-09-11）

基于 `dc7eef8` 的本次修订已用正式构建实测：390px 中文、英文、阿语均无页面横向溢出；出发前准备直接显示六项原说明，没有勾选或完成计数。介绍正文实际为 15px / 24.75px，开头句为 16px，章节提示展开后隐藏，“广州与你们”的开头句仅出现一次。320px 阿语路线与全员行程无横向溢出，广州塔竖图完整按比例显示；1280px 表格完整，正文不再随桌面放大。人员切换、日期跳转、酒店地址复制已实际操作，浏览器未记录运行错误。

TypeScript、ESLint、指南检查、65 项降噪检查、28 项行程检查、18 个视图渲染及正式构建通过。`lib/trip-data.ts` 与 `lib/person-day-plan.ts` 均保持基线内容。上述记录是实现检查，不代表用户对新排版作了视觉认可；发布提交以线上 `source-version.json` 为准。

## 上一版（dc7eef8）的发布前验收 —— 那一版随后被用户否定

> 以下是当时跑过什么的记录。**跑绿 ≠ 用户满意**：用户看了那一版之后否定了它的
> 交互与排版（分组清单、放大字号），现行做法见下一节。清单相关的部分已经作废。

2026-09-11，主窗口对当时那次提交的正式构建完成浏览器验收：390px 中文、英文、阿语无页面横向溢出；阿语页头为 111px；320px 阿语指南与全员表格、1280px 全员表格均完整显示。日期跳转定位至对应日期首行。路线六张图片加载正常，广州塔竖图保留全塔。酒店地址按钮显示“已复制”。

TypeScript、ESLint、正式构建、指南检查、行程检查、降噪检查及 18 个视图渲染当时全部通过；当时另有 29 条勾选清单断言，随清单一起删除。发布版本与源码提交的对应关系由线上 `source-version.json` 记录。

## 当前排版与阅读规则（2026-09-11 用户第三次明确，取代前两轮）

用户看过上一版后的原话：「你不要给我搞什么出行随手查这种乱七八糟花哨的功能啊，
就之前那种介绍的形式就行了……页面文字这么大……排版……层级也不清晰。越改越差了。」

**先说清楚一件事**：前两轮结尾写的「检查通过」只代表 tsc / lint / 那几个检查器跑绿了，
那是运行验证，**不等于用户认可这个设计**。检查器证明不了好不好看，别再拿它当验收结论。

### 现在有效的规则

- **开篇是一段欢迎**（2026-09-11 用户确认）：指南最顶上一张照片 + 「欢迎来到广州」
  + 两句话，**默认就看得见**，不折叠、不加按钮、不占满屏（标题 20px、正文 15px）。
  文案是 `guide-tab.tsx` 里的 `WELCOME` 常量，三语，没有新事实；`lib/trip-data.ts` 不动。
- **开篇照片改成明亮的珠江城市全景**（2026-09-11 用户确认，取代怀圣寺屋檐那张）：
  用现有的 `skyline.jpg`（1200×492），`IMG.skyline` 与图片署名不变，不下载新图、不做 AI 图。
  **按自然比例完整显示，不裁楼、不拉成窄条**：手机满宽，桌面左图右文、图宽限 26rem，
  高度自己按比例算，所以两边取景一样 —— 不再写死 220px 高。
  这张照片整份指南只出现这一次：**「这座城有多大」一章已去掉重复的同一张图**，
  文字与统计数字照旧；「广州与你们」也不硬塞回旧的屋檐图。
- **章节顺序按一次旅行读下来排**（同上，取代「酒店块第一 / 城市故事折在后面」的旧做法）：
  欢迎 → 出发前准备 → 酒店与联系 → 机场中文地址 → 琶洲 → 广州与你们 → 这座城有多大 →
  科技就在身边 → 礼拜与清真餐 → 食在广州 → 商圈 → 半日路线 → 常见的文化差异 →
  可复制中文短句 → 免费行李额 → 官方来源。
  欢迎之后紧接着出发前准备，酒店块不挡在中间；实现上只是两个 `Accordion` 容器
  （准备一个、其余一个），没有新状态。
- 指南是**一列介绍章节**：一章一张卡，复用 `Accordion`，点开整章连着读完，
  默认全部收起（`defaultValue={[]}`）。每章只出现一次，没有分组名，也没有目录导航。
- **「食在广州」里有一节特色餐饮品牌推荐**（2026-09-11 用户确认）：
  一句导语 → 品牌推荐 → 原有的饮食介绍与十二道菜（一条没删，只是排在推荐之后）。
  数据在 `lib/dining-brands.ts`，排版在 `components/trip/dining-brands.tsx`。
  规矩写在数据文件开头，改之前先读：
  - **按口味分主题，不按商圈**：粤菜与早茶 4、啫啫煲 2、川菜 2、火锅与海鲜 3、
    亚洲其他口味 8、茶饮 2，共 **21 个品牌**。同一个品牌只出现一次
    （芒果树原来有两条分店信息，已合成一条，人均写成区间）。
  - **推荐的是品牌，不是某一家分店**：不写门店地址、电话、营业时间。
    中文店名可复制（复用 `CopyChinese` 新增的 `inline` 变体，只有一行名字加一个按钮，
    不是整张大地址框；原有用法不变），到高德或大众点评里搜身边那一家。
    **中文视图里店名只出现一次**：标题就是中文店名，复制按钮跟在标题旁边（`hideChinese`）；
    英文、阿语的标题是译名，下面单独露出中文检索名 —— 那是给店员看的，不是重复。
  - **人均是用户给的参考金额**，不声称是刚核过的现价。单位（人民币 / CNY）在导语里
    说一次，每行只写「约 96 元/人 · CNY 96/person · 96 يوانًا/للفرد」；
    阿语下数字用 `bdi` 保证顺序。
  - **每个品牌四行左右**：名字、人均、一句特色、一到两道菜（行内引子只写「可试：」，
    冒号按语言走：中文全角，英阿半角加空格）、复制按钮。
    不写「谁吃过」「几分」「最好吃」「保证正宗」，不声称哪家清真，也不推荐猪肉或酒的菜。
  - **导语就两句，各一次，不解释「每家写了什么」**（下面一眼看得见）：
    一句「复制中文品牌名去高德 / 大众点评搜门店，人均是人民币参考，菜品与价格以门店为准」，
    一句「需清真餐或有过敏、忌口，向店员确认食材、汤底及烹调用酒」，
    且要在挑店**之前**看到 —— 不逐行加警告。
    `FOOD_ADVICE` 原来那条清真 / 过敏与它重复，指南页把那一条滤掉了（数据文件不改），
    机上特殊餐那条照旧显示。
  - **21 个复制按钮的可访问名字各带自己的店名**，不能都念「复制 中文店名」；
    复制到剪贴板的仍然只有中文店名本身。
  - **来源收在章尾一个「菜品来源」折叠里**，每个品牌一条，不做 21 个折叠按钮。
    菜名只做简短转述，不抄长评、不编新招牌、不承诺某家分店一定供应。
    来源要能被打开、能在页面上找到那两道菜：陶陶居原来那份 PDF 里蛋挞不好定位，
    已换成同时列出大虾饺与现烤蛋挞的携程菜单页。
- **不做分组、不做工具**：「出行随手查 / 认识广州」两个分组标题、目录式导航、
  勾选清单、完成计数、localStorage 记录 —— 全部已删除，不要再加回来。
- **出发前准备就是文字**：支付、打车、地图、上网、随身物品、天气各一个小标题，
  下面直接是原有的三语说明。分人可见性不变：Chuck 借给 Mohamed 的中国卡与备用金
  只属于 Ahmed / Mohamed，公共说明对 Reham 一律可见。
- **字号（本轮实施，只写在 `components/trip/ui.tsx` 的 `GUIDE` 里）**：
  介绍正文 15px / 1.65、章标题 17px 半粗、小标题 15px 半粗、副说明与来源 13px，
  每章开头那句 16px 半粗（不再是 22 / 26px 的衬线大字）。**桌面不再放大一档**。
  行程表：日号 17px、单元格正文 14px（桌面同样 14px）、弹窗正文 15px；统计数字 24px。
  正文限宽 44rem，表格容器仍是 1200px。层级靠字号、字重、段距区分，不靠一层层套框。
  用户明确的是「字太大、层级不清」；**上面这组具体数值是本轮的实施选择，待页面核验**。
- **15px 是对「正文不小于 16px」这条默认习惯的一次例外**，依据是用户点名字太大。
  根字号仍是 16px；`<select>` 等输入控件仍保持 16px，否则 iOS 聚焦会放大整页。
- **路线**：收起来只有路线名（16px）+ 时长（13px）；照片和「适合什么」放在展开之后，
  各出现一次。广州塔是竖图，按比例放全，不裁掉塔顶塔底。
- **章节提示只在折叠时显示**：有几章的提示就是正文开头那句（「广州与你们」），
  展开后会连读两遍；展开时收掉提示，标题一直在。这也是实施调整，不是用户逐项要求。
- 页头（印章 + 标题 + 日期区间 / 人员下拉 + 语言）、三列行程表、默认 Reham、
  URL 人员与语言参数、合并规则、日期跳转、44px 触控，一律不变。

### 仍然有效的内容与照片修正

- `cbd.jpg` 有 500px 水印、`tianhe.jpg` 认不出地点，两张都不再使用；花城广场用
  `opera.jpg`，天河路用 `taikoo.jpg`，广州塔用 `tower.jpg` 且不裁。
  **不裁剪、不抹水印、不下载新图**；弃用的文件与授权信息留在仓里。
- 路线文案：花城广场删掉「北端」与「打车过江即到」；广州塔删掉「世界第二高」、
  改掉「沿江走到对岸」、票务写官网 09:30–22:30 并说明总体营业时间不等于各项目结束时间；
  珠江夜游改成「船程约 60 分钟，另留往返及候船时间」并要求订票时核对码头、船名、路线；
  天河路删掉排名与客流数字，改成几家商场的空间特点。三语同步。
- 商圈文案不留无依据判断（「有意错开」「价格带最低」「没人用的空间」等）。
- 酒店那块保持精简：酒店名一次、中文地址一次（可复制、举给司机看）、前台电话。

### 用户明确说过的（当规则用，不要再试探）

| 用户的反馈 | 何时 |
|---|---|
| 不要「出行随手查」这类分组与工具化功能，就要之前那种介绍形式 | 2026-09-11 第三轮 |
| 页面文字太大 | 2026-09-11 第三轮 |
| 排版层级不清 | 2026-09-11 第三轮 |
| 页头不要常驻六个人的横滑按钮 | 2026-09-11 第一轮 |
| 指南不要十几张一模一样的白卡 | 2026-09-11 第一轮 |
| 指南首屏要有欢迎开篇，下面按旅行阅读顺序排 | 2026-09-11 第四轮 |
| 欢迎那张屋檐照片不好看，换明亮的珠江城市全景，按自然比例不裁建筑 | 2026-09-11 第五轮 |
| 「食在广州」里加特色餐饮品牌推荐，按主题不按商圈 | 2026-09-11 第五轮 |

因此删掉的：两个分组标题与分组容器、出发前准备的勾选清单 / 完成计数 / 本机记录。

### 本轮据此做的实施调整（我们的选择，待实际页面核验）

- 具体字号那一组数值（正文 15px、章标题 17px、小标题 15px、副说明 13px、开头句 16px、
  表内 14px、日号 17px、统计数字 24px），以及桌面不再放大一档。
- 路线折叠条改成只有路线名与时长，照片和「适合什么」移到展开里各出现一次。
- 章节提示只在折叠时显示，避免与正文开头那句重复。
- 正文限宽 44rem。
- 欢迎那段的具体写法：手机满宽、桌面左图右文（图宽 26rem，高度按 1200×492 自己算）、
  标题 20px、两段 15px 正文，以及各章之间的先后（例如琶洲排在广州与你们之前）。
  用户确认的是「要欢迎开篇 + 按旅行阅读顺序 + 换成明亮的珠江全景、按自然比例」，
  这些尺寸细节是实施选择。
- 餐饮推荐的排法：主题小标题 + 一句导语，品牌之间细线分隔，每个品牌四行，
  复制按钮用 `CopyChinese` 的 `inline` / `hideChinese` 变体，来源折在章尾。
  用户确认的是内容与边界（含第六轮逐条核过的菜名、译法与提示合并），这套版式是实施选择。

这些都还没有得到用户的视觉认可，**检查器跑绿也不算认可**；有异议就按用户当次的话调整。

## 当前依据（2026-09-11 用户确认）

核心读者为 Citystars 品类招商负责人 Reham。此行的主线是参加 MINISO 会议、了解货盘与旗舰店型；自由时间的商圈与城市探索由客人选择。页面要体面、实用、有趣，不向客人暴露内部商业目的，不指导其写报告，也不把逛商圈与给予 MINISO 商业政策建立因果关系。MINISO 的规模与实力由会议现场介绍，网页不另添宣传章节。

保留已认可的默认 Reham、三列行程、正常大小写姓名和三语切换。指南先给欢迎开篇，再给出发前准备与酒店地址/电话/接送联系（顺序见上一节，取代本节原来写的「先酒店后准备」）。文化、城市规模、科技、美食与商圈按需展开。复用现有 Accordion、CopyChinese、SourceLink；字号按上一节「当前排版与阅读规则」的现行尺度（正文 15px），这一条取代本节原来写的“正文至少 16px”。以下决定替代 DESIGN_NOTES 中“城市长文必须展开”等旧指示。

## 数据边界

- 支付、打车、地图与上网指引必须在 Reham 视图可见；不能因内部员工安排而隐藏公共说明。
- Chuck 借给 Mohamed 的中国 SIM 与给 Ahmed/Mohamed 的备用金仅归该两人。Reham 的网络方案不假定由公司提供。
- 日程事实留在原有数据与派生函数中。本轮不更改航班、免费行李额、日期或分房安排。
- 科技介绍提供体验入口和确认方式；不能把未核实的酒店机器人服务、无人商店或飞行体验写成已经安排。
- 商圈路线是游览顺序示意，明确不是地理地图。两商圈分开安排，提供中文目的地；交通时间使用出发当天地图，不能靠示意图估算。
- 车程一律不许给「即到 / 很近 / 短车程」这类承诺：没查过路况，一律写「出发前在地图里查当天车程」。
- 广州塔 09:30–22:30 是官网公布的总体营业时间，不能当作每个高空项目的结束时间；票价分层分项目，未核实不写具体数字。
- 珠江夜游不能说所有班次都途经广州塔：码头、船、路线各不相同，页面给的是订票时该核对什么。
- 页面上的照片必须是能认出地点的实景，且**不裁剪、不抹水印**；带水印或认不出地点的图直接不用，不为凑图硬上。
- 不加未实测的功能按钮（例如地图 URI 跳转）：文档查到不等于在真机上能打开。
- 已撤销：埃及/广州经济强弱比较、全球仅六座美食之都、擎朗为广东公司、完全没有业态竞争、将历史传说与准确年代混写。

## 核实来源

- 支付、外卡、手机号注册、打车与通信准备：https://english.www.gov.cn/2025special/bizexpatsinchina2025
- 高德移动应用：https://m.amap.com/applink/appstore.html?schema=amapuri%3A%2F%2Frootmap%3FsourceApplication%3Dgrowth_guanwang
- 酒店地址与前台电话：https://www.ihg.com/intercontinental/hotels/cn/zh/guangzhou/canec/hoteldetail
- 天河城地址：https://ghzyj.gz.gov.cn/ywpd/slgsnew/content/post_10332177.html
- 太古汇地址、石牌桥站连接：https://www.taikoohui.com/en/about-us/about-us/contact-us
- 顺德与2014年入选：https://www.unesco.org/en/creative-cities/shunde
- 无人驾驶服务：https://www.weride.ai/services
- 酒店机器人能力（不是洲际安装证明）：https://www.pudurobotics.com/en/solutions/hospitality
- 广州塔营业时间 09:30–22:30 与在线购票（官方网站，主窗口本轮核实）：https://www.cantontower.com/?lang=zh
- 广州塔与海珠区游览指引（海珠区政府，写的是第三高，页面据此删掉「世界第二高」）：https://www.haizhu.gov.cn/hzdt/ztlm/tzhz/rjhj/lyjd/csmp/content/post_9190964.html

## 源码与发布

用户本次指定的网页为 https://minisochuck-commits.github.io/guangzhou-trip-2026/ 。该仓 main 是静态生成物。源码基线为本地 83193d0；它的构建分包 trip-view-BWWt0mWU.js 与 GitHub 已发布 2c3e8cc 的同名分包 SHA256 一致：d4a13ac9883bd7ae398c680239349f5a21be3570e41b6bb2bc5f0ec7ba878f62。

本轮 Sites get_site 与凭证刷新均返回 project_not_found，未修改 Sites 身份、权限或已有站点。源码保存在同一 GitHub 仓的 codex/guangzhou-guest-source 分支，静态文件从源码构建后更新 main，普通快进推送，禁止旧 deploy-pages.sh 的强推重建历史方式。

构建：npm run build。启动生产预览 npm run start -- --port 8797，执行 node scripts/make-static.mjs http://127.0.0.1:8797 static。静态发布目录仅同步 static 中的产物；不得发布 node_modules、构建服务端配置或凭证。版本对应关系写入静态目录 source-version.json。

验证：npx tsc --noEmit、npm run lint、node scripts/check-guest-guide.mjs、node scripts/check-journeys.mjs、node scripts/check-noise.mjs、node scripts/check-render.mjs（三语 × 六视图服务端渲染冒烟，不起服务、不开浏览器）。

**检查器只证明内容没掉、被否掉的做法没回来，证明不了好不好看。**
随后仍要在真实浏览器看 390px 中文 / 英文 / 阿语：字号是不是收住了、章标题与正文的层级、
展开一章能不能连着读完、出发前准备是一段文字而不是清单、路线收起只有名字与时长、
页头下拉换人与语言切换、日期跳转与 sticky、复制按钮、无横向滚动；
发布后核 GitHub Pages build commit 并复查线上。
